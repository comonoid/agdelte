# Полный план отладки кодовой базы Agdelte

**~63K LOC**, 268+ файлов, 124 темы. Колонка **П** — количество подходов к анализу. Пустая = ещё не смотрели.

## Как работать с этим документом

### Быстрый старт

```
# 1. Запусти тесты — если что-то красное, это уже баг
node test/runtime.test.js
node test/fuzz-runtime.mjs
node test/test-clone-formats.mjs
node test/gl-math.test.js
node test/smoke.test.js

# 2. Выбери тему из таблиц ниже (пустая колонка П = ещё не смотрели)
# 3. Используй промпт-шаблон
# 4. Найденный баг → исправь → добавь тест → прогони тесты
# 5. Обнови колонку П в этом документе
```

### Промпт-шаблон (копируй и подставляй)

Для JS/Haskell (код можно запустить):
```
Прочитай [файл], строки [N-M] (тема [ID]).
Найди всё что неправильно. Для каждой находки:
- Номер строки
- Конкретный input/сценарий который ломается
- Ожидаемое vs фактическое поведение
Затем исправь все подтверждённые баги.
Каждый фикс проверь: node -c [файл] + запуск тестов.
```

Для Agda (нельзя запустить, но можно проверить логику):
```
Прочитай [файл] (тема [ID]).
Проверь: 1) каждый конструктор/postulate соответствует JS runtime
         2) логические ошибки в алгоритмах
         3) пропущенные edge cases
Для FFI: сравни сигнатуру postulate с реализацией в JS/Haskell.
```

### Выбор следующей темы

Приоритет:
1. 🔴 Темы с П = пусто и высшим приоритетом (FFI: A21-A23, Haskell: H1-H6, JS: R6, G4, G6, G10, G14)
2. 🟡 Agda Core (A1-A20) — фреймворк, от которого зависит всё
3. Темы с П = 1, которые давно анализировались (могли появиться новые баги)
4. 🟢 Examples, Scripts — низкий приоритет

### После нахождения бага

1. Исправить код
2. Написать тест в `test/fuzz-runtime.mjs` или `test/runtime.test.js`
3. Прогнать все тесты: `node test/runtime.test.js && node test/fuzz-runtime.mjs && node test/gl-math.test.js && node test/smoke.test.js`
4. Обновить колонку П в этом файле (увеличить на 1)

### После анализа темы (даже без бага)

- Записать наблюдения в `debug-notes.md` (секция по ID темы): недостающие тесты, подозрительные места, неточные комментарии, идеи улучшений

### Ключевые файлы проекта

| Файл | Назначение |
|------|-----------|
| `debug-plan-all.md` | Этот документ — каталог тем и прогресс |
| `bugstrat.md` | Стратегия и покрытие фаззингом |
| `debug-notes.md` | Заметки по темам: недостающие тесты, подозрительные места, идеи |
| `CLAUDE.md` | Инструкции по проекту (NixOS, generated files, build commands) |
| `package.json` | Build scripts (`npm run build:*`, `npm run css:*`) |

### Контекст: что уже сделано

**Раунды 15-29** (24 бага в JS runtime):
- Shader uniforms (3), normal matrix transpose (2), textured no-lights (1), blend state (1)
- zoomRT try/finally (2), mutable cycle (1), object-format cloneSlot (1)
- Event deduplication (1), event fingerprint (2), P1 freeze (1)
- BigInt serialization (1), httpPost Content-Type (1)
- deepEqual depth direction (1), ensureString robustness (1), listToArray crash guard (1)

**Раунды 30-31** (~50 багов в ~70 файлах):
- Taylor sin/cos range reduction (5 файлов), event bubbling (3), :focus-visible (12 CSS)
- SVG: DatePicker, Filter×3, Path, Smil, Axis, Connector×3, RangeSlider, Slider, Gauge×4, Sparkline, Pie, Radar, Heatmap×2, Bar, Area, Treemap, Sankey×3, Network
- HTML: Spinner×11, Pagination, Slider orient, TreeView CSS, border-radius×2
- WebGL: Grid, ringFacing, applyActive, pushButton, Bar3D, colorPicker, Scatter3D×2, Transform gizmo
- JS runtime: flushP2 try/catch, localStorage try/catch, BOOL_ATTRS, WorkerPool handled, slerpQuat clamp, durationMs guard, Safari fallback budget
- Haskell: Process timeout 30s, allocShared arity
- Examples: WebSocket pendingMsg, PanZoom swap, Router 404
- Framework: Task.agda ok/er rename, Smil.agda idAttrs, Node.agda cmd comment
- Build: CSS hash check, anim-data exit code, typecheck/verify missing modules
- Spring isSettledRange, Widget comment, ProgressRing normalize, Gauge labelColor

**Раунд 32** (11 финальных backlog-пунктов):
- JS: SMIL conditional show start, httpPost JSON regex heuristic, legacy debounce/throttle удалён, mat4LookAt parallel up fallback, isTransparent instanced type
- Agda: rangeSliderCompact clamping, breadcrumbBind last-item, Morph comment fix, Treemap zero-value + fuel guard
- Haskell tests: timeout recovery (3 теста), concurrent access (4 теста)

584 тестов зелёные (39 runtime + 137 fuzz + 26 GL math + 382 smoke + 20 clone) + 28 Haskell тестов.
Все 124 темы проанализированы минимум трижды (П≥3). Backlog пуст.

---

## 1. JavaScript Runtime (9,775 LOC)

### reactive.js (2,174 LOC)

| # | Тема | Строки | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|--------|-----|---|------------|------------|
| R1 | SMIL + namespace | 20-97 | 80 | 3 | Syncbase ref cycle в startSmilAnimations; foreignObject сбрасывает namespace обратно на HTML — а что с вложенным svg внутри foreignObject? Правильный ли порядок: сначала собрать все анимации, потом запустить по зависимостям? Может ли RAF не выполниться если container ещё не в DOM? **Р1-2:** Чисто. foreignObject namespace restoration корректна, вложенный svg работает. **Р3:** **Исправлено**: startSmilAnimations вызывается при when/whenT conditional show. SMIL syncbase semicolon-separated begin — только первый ref match. | Тест SMIL с множественными syncbase refs (begin="a.end; b.end"). SMIL внутри вложенного foreignObject→svg. Тест re-show после leave transition. |
| R2 | executeTask + executeCmd | 98-280 | 180 | 3 | `querySelector(sel)` — а если sel содержит спецсимволы (кавычки, скобки)? `focus` после delay — элемент мог быть удалён. `httpGet`/`httpPost` — что если url пустой или содержит newlines? `attempt` — task chain без ограничения глубины? `getItem` — `localStorage.getItem` может бросить `SecurityError` в private browsing. `wsSend` на CLOSING WebSocket. `channelSend` на terminated worker. `readClipboard` fallback пустая строка — caller не отличит успех от ошибки **Р1-2:** **Исправлено**: localStorage getItem/setItem/removeItem обёрнуты в try/catch (SecurityError в private browsing). **Р3:** Чисто. querySelector try/catch, wsSend CLOSING guard, focus null check — всё корректно. | Тест attempt task chain глубина >100. channelSend на terminated worker. readClipboard fallback семантика (пустая строка vs ошибка). |
| R3 | wrapMutable + reconcile + cloneSlot | 304-450 | 150 | 3 | `wrapMutable` object-format: wrapper `{ [ctor]: fn }` — при reconcile `.apply()` вызывается? `cloneSlot` для object-format (исправлено r28) — проверить что fix корректен. `reconcile` при смене arity — `oldSlots.length !== newSlots.length` — а если newSlots null? Shared reference: два слота → один объект, мутация одного видна через другой. `_visited` WeakMap не чистится — GC handles? **Р1-2:** cloneSlot r28 верифицирован. reconcile корректен. Shared ref через WeakMap OK. **Р3:** Чисто. reconcile null guard, WeakMap cycle detection, cloneSlot object-format. | Тест wrapMutable с >10 уровней вложенных Scott-encoded values. reconcile при конструкторе→примитив transition. |
| R4 | createScope + destroyScope | 460-510 | 50 | 3 | `destroyScope` — копирует `children` перед итерацией (`[...scope.children]`), но что если destroyScope вложенного child модифицирует `scope.children` через splice? Гонка. `abortCtrl.abort()` — что если listener callback уже в callstack? `conditionals` cleanup — `enterTimeoutId`/`leaveTimeoutId` могут быть 0 (falsy но валидный ID) — `clearTimeout(0)` безопасен? **Р1-2:** Чисто. destroyScope children copy + splice безопасен. clearTimeout(0) — no-op. **Р3:** Чисто. Children copy, abort safe, clearTimeout(0) no-op. | Тест destroyScope во время whenT leave animation timeout. Гонка abort + dispatch в одном microtask. |
| R5 | Priority dispatch + batch | 620-870 | 250 | 4 | `applyBatch` — `preBatchModel` захвачен до цикла, но `model` мутируется в цикле — следующий `cmdFunc(msg)(preBatchModel)` видит старую модель, это by design? `MAX_BATCH_SIZE = 10000` — `_droppedP1`/`_droppedP2` считают drops но нигде не сбрасываются. `flushP1` try/catch (исправлено r28) — а `flushP2` тоже вызывает `processBatch` без catch (строка 759) — если крэш в P2? `dispatchBackground` fallback `setTimeout` для Safari — timeout 100ms, а в `flushP2` `timeRemaining()` всегда 50 — обрабатывает ли это больше 1 сообщения? **Р1-2:** **Исправлено**: flushP2 try/catch добавлен. Safari fallback заменён на performance.now() budget 8ms. **Р3:** **Исправлено**: Safari dispatchBackground budget приведён к performance.now() 8ms. | Тест P2 queue ordering при crash recovery. Тест MAX_BATCH_SIZE=10000 drop counter reset. |
| R6 | renderNode: elem, bind, text, empty | 881-930 | 50 | 3 | `bind` — `extract(model)` при первом рендере, но model может быть zoomRT-projected — правильный ли тип? `text` — создаёт TextNode из str, но если str содержит `null`/`undefined`/`BigInt` — textContent обработает? `elem` — `listToArray(attrs)` и `listToArray(children)` — а если attrs не list? Namespace для `math` элемента — все ли MathML теги создаются с MATHML_NS? **Р1-2:** Чисто. Namespace svg/math/foreignObject корректен. listToArray защищён. **Р3:** Чисто. MathML namespace OK, BigInt textContent OK. | Тест elem с attrs=не-list (null, undefined, number). bind extract на зumRT-projected model с wrong type. |
| R7 | renderNode: when, whenT | 930-990 | 60 | 3 | `when` — `!!cond(model)` — а если `cond(model)` возвращает BigInt 0n? `0n` falsy но не `false`. `whenT` — `enterClass` applied и `duration > 0` для setTimeout — а что если duration = 0.5 (дробное)? `setTimeout(fn, 0.5)` → 0 или 1? `rendered` flag vs DOM state — могут разойтись при быстром toggle? **Р1-2:** Чисто. BigInt 0n корректно falsy. setTimeout(fn, 0.5) → 0 или 4ms. **Р3:** Чисто. whenT leave race harmless (detached node replaceWith no-op). | Тест 100 быстрых toggles when — memory leak от comment nodes. whenT duration=0 edge case. |
| R8 | renderNode: foreach, foreachKeyed, scope, scopeProj, zoomRT, glCanvas | 990-1120 | 130 | 3 | `foreach` — `BigInt(idx)` — а если idx > Number.MAX_SAFE_INTEGER? `foreachKeyed` — `keyFn(item)` может вернуть object/function — `Map.set` по reference. `zoomRT` try/finally (исправлено r22) — проверить что finally корректно восстанавливает. `glCanvas` — `dispatch` captured at render time, а `__glDispatch` — тот же? `scope` — `fingerprint(model)` вызывается дважды: при рендере и при первом update — а если fingerprint side-effectful? **Р1-2:** Чисто. zoomRT try/finally r22 корректен. **Р3:** Чисто. Object keys в foreachKeyed — semantic limitation. | Тест foreachKeyed при 10000 items — DOM reorder performance. glCanvas dispatch identity при re-render. |
| R9 | setAttr + applyAttr | 1122-1260 | 140 | 3 | `setAttr` — `BOOL_ATTRS` set: полный ли список? Отсутствует `inert`, `popover`? `name === 'value' && 'value' in el` — а для `<progress>`, `<meter>` — свойство `value` есть но семантика другая. `onValue` wheel — `e.deltaMode` 0/1/2 — правильный ли множитель для mode 2 (`window.innerHeight`)? `onValueScreen` — для touch events `e.clientX` undefined — fallback '0,0'. `onKeyFiltered` — `keyArray.includes(e.key)` — если keys содержит '' (пустую строку)? **Р1-2:** **Исправлено**: BOOL_ATTRS — добавлены inert, popover. **Р3:** Чисто. progress/meter value как HTML attribute OK. deltaMode=2 multiplier correct. | Тест onKeyFiltered с пустой строкой в keyArray. Touch events onValueScreen без clientX. |
| R10 | updateScope + lists | 1276-1690 | 410 | 3 | `updateScopeImmediate` try/finally для zoomRT (исправлено r22). `ensureSlots` — `detectSlots` вызывается с `newModel`, а binding создан с `oldModel` — если конструктор сменился, slots = null, повторное обнаружение. `updateKeyedList` — `oldKeyMap.set(oldItems[i].key, oldItems[i])` — если два oldItems имеют одинаковый key, первый теряется. `updateUnkeyedList` — `parent.insertBefore(itemNode, insertBefore)` — если insertBefore уже не дочерний parent (removed) — DOM throws. `makeTruncatedMarker` — создаёт `<li>` — а если parent не `<ul>`/`<ol>`? **Р1-2:** Чисто. Duplicate keys — keep-last. insertBefore stable. **Р3:** Чисто. Duplicate keys keep-last, insertBefore stable. | Тест updateKeyedList при simultaneous add+remove+reorder. ensureSlots при constructor change mid-animation. |
| R11 | serializeEvent + updateSubscriptions | 1691-1790 | 100 | 4 | `serializeEvent` — `onKeys` fingerprint исправлен (r24), но другие handlers: `onKeyDown`/`onClick` — handler function identity не в fingerprint — если handler closure пересоздаётся каждый вызов subsFunc, fingerprint не меняется но handler устаревает. `parallel`/`race` — fingerprint `'parallel'`/`'race'` без содержимого — при смене внутренних events не переподписывается. `Proxy` fallback — `target[prop] || fallback` — что если handler возвращает пустую строку `''` (falsy)? **Р1-2:** Чисто. Handler identity не в fingerprint — known limitation. **Р3:** Чисто. Handler identity limitation known. | Тест Proxy fallback с handler возвращающим '' (falsy string). parallel fingerprint при динамической смене sub-events. |
| R12 | BigLens + time-travel | 1840-2020 | 180 | 3 | `serializeModelValue` — `seen` Set для cycle detection, но functions не добавляются в seen — может ли function→function→... зациклиться? `MAX_DEPTH = 64` — достаточно? `JSON.stringify` на большой модели — может ли заблокировать main thread? `timeTravel_undo` — `historyPast.shift()` — restore model без `wrapMutable` — restored model mutable? `timeTravelDispatch` использует `dispatch` (P1), а не `dispatchImmediate` — undo через rAF, не мгновенный **Р1-2:** Чисто. timeTravel_undo restore model is already mutable via cloneSlot. **Р3:** Чисто. Function cycles safe via depth limit 64. | Тест serializeModelValue performance на модели с 10000 slots. timeTravelDispatch через rAF vs immediate — user-visible delay. |
| R13 | hotReload + destroy + mountReactive | 2023-2175 | 150 | 3 | `hotReload` — `rootScope.abortCtrl = freshScope.abortCtrl` — а старый abortCtrl уже aborted через destroyScope? Новые listeners на старом scope? `destroy` — `cancelAnimationFrame(p1RafId)` — а если `p1RafId` === 0 (initial value)? `cancelAnimationFrame(0)` — no-op? `mountReactive` — `import(modulePath)` — если путь relative, зависит от base URL страницы. Error display — stack trace может содержать sensitive paths **Р1-2:** Чисто. hotReload abortCtrl lifecycle корректен. cancelAnimationFrame(0) — no-op. **Р3:** Чисто. hotReload SMIL restart, cancelAnimationFrame(0) no-op. | Тест hotReload при pending P2 queue messages. mountReactive error display sanitization. |

### events.js (775 LOC)

| # | Тема | Строки | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|--------|-----|---|------------|------------|
| E1 | interpretEvent combinators | 46-375 | 330 | 4 | `merge` — если e1 synchronous и dispatches, а e2 ещё не subscribed — порядок подписок. `debounce` — `lastMsg` не обнуляется при unsubscribe — closure holds reference. `throttle` — `pendingMsg` аналогично. `foldE` — `state = step(inputVal)(state)` — если step возвращает promise? `mapFilterE` — `maybeResult({just, nothing})` — а если maybeResult object-format Scott? `switchE` — `currentSub.unsubscribe()` перед созданием нового — а если unsubscribe triggers dispatch? `parallel` — `results` Array fixed size, done Array — а если event dispatches multiple times? `race` — `subs.forEach(s => s.unsubscribe())` вызывается из `raceDispatch` — а subs ещё наполняется в цикле. `onKeys` — пустой pairs list → пустой keyMap → addEventListener всё равно добавляется **Р1-2:** Чисто. race safe (finished flag). parallel done[i] OK. switchE — латентная хрупкость при re-entrant dispatch. **Р3:** Чисто. foldE с promise — model станет Promise (caller bug). switchE re-entrant dispatch latent. | Тест merge порядок подписок — e1 sync dispatch before e2 subscribed. debounce/throttle lastMsg/pendingMsg cleanup при GC. |
| E2 | makeLeafHandlers | 378-670 | 290 | 3 | `interval` — `setInterval` — drift accumulation на длинных интервалах. `timeout` — `ensureNumber(ms)` на BigInt — `Number(9007199254740993n)` потеря precision. `springLoop` — `position` captured как `ensureNumber(position)` но `range` тоже `ensureNumber(position)` — double conversion. `httpPost` auto-detect JSON — `body[0] === '"'` — строка `"not json` пройдёт JSON.parse? Нет: JSON.parse бросит, catch проглотит. Но: `try { JSON.parse(body) }` парсит ВЕСЬ body для проверки Content-Type — может быть медленно на большом body. `wsConnect` — `ws.onclose` удаляет из `wsConnections`, но если `onclose` вызван ПОСЛЕ `unsubscribe` — double delete. `workerChannel` — `prev.terminate()` — а если prev не terminated а в середине работы? **Р1-2:** Чисто. springLoop без double conversion. wsConnect onclose safe. **Р3:** **Исправлено**: httpPost JSON auto-detect заменён на regex heuristic (/^\s*[\[{]/). | Тест httpPost с body начинающимся с `[` но невалидным JSON. wsConnect double-close race. interval drift measurement на 10-минутном интервале. |
| E3 | interpretSub + helpers | 672-740 | 70 | 3 | `mkKeyboardEvent` — поля: key, code, ctrlKey, altKey, shiftKey, metaKey, repeat, location — порядок должен совпадать с Agda record. `BigInt(e.location)` — `e.location` всегда integer? На мобильных может быть 0. `mkMouseEvent` — `BigInt(e.clientX)` — спецификация говорит `double`, но браузеры возвращают integers для MouseEvent. А для PointerEvent? Если код когда-нибудь перейдёт на PointerEvent — fractional clientX сломает BigInt. `dispatchWorkerProgress` — `data.type === 'progress'` — а если worker шлёт `{type: 'progress', value: undefined}`? `String(undefined)` = `'undefined'` **Р1-2:** Чисто. mkKeyboardEvent/mkMouseEvent поля совпадают с Agda record. **Р3:** Чисто. Field order match, BigInt location safe. | Тест mkWheelEvent (если добавится). Keyboard repeat flag на мобильных. Touch event coordinate precision. |
| E4 | Exported utilities | 742-775 | 30 | 3 | `debounce` exported — возвращает function без cancel method. `throttle` exported — аналогично. Обе не чистят таймеры при garbage collection. Используются ли вообще за пределами events.js? **Р1-2:** Чисто. debounce/throttle используются только в тестах. **Р3:** **Исправлено**: legacy debounce/throttle удалены из events.js. | Нет — тема закрыта. Функции удалены. |

### agda-values.js (638 LOC)

| # | Тема | Строки | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|--------|-----|---|------------|------------|
| V1 | Probing | 14-200 | 190 | 3 | `getCtor` — Proxy handler `get(_, name) { return (...args) => { ctor = name; }; }` — а если Scott function вызывает handler с Symbol key (e.g., Symbol.toPrimitive)? `getSlots` — возвращает args array из Proxy — а если constructor не вызывает handler (returns early)? `probe` — аналогично. `_slotProbe` — global shared Proxy — thread-safe? (JS single-threaded, OK). `probeSlots` для object-format — `value[keys[0]](_slotProbe)` — а если keys[0] === '__proto__' или 'constructor'? **Р1-2:** Чисто. Proxy Symbol keys теоретическая проблема, Agda codegen не использует Symbol. **Р3:** Чисто. __proto__ key в Proxy — безопасно. | Тест probe на value, который бросает при вызове. getCtor с Proxy-wrapped Scott function. |
| V2 | deepEqual + slot tracking | 203-389 | 190 | 4 | `deepEqual` — `return true` при превышении глубины (исправлено r29). `a(probeA)` / `b(probeB)` — если a или b не чистые (side effects при вызове) — deepEqual может мутировать state. `detectSlots` — sentinel Symbol injected в каждый слот — а если extract function caches result? First call returns baseValue, sentinel call returns cached same value — dependency missed. `changedSlotsFromCache` — `scope.cachedArgs = newArgs` — mutates scope in-place — а если changedSlotsFromCache вызван дважды в одном update cycle? **Р1-2:** deepEqual depth r29 верифицирован. detectSlots sentinel OK для чистых projections. changedSlotsFromCache идемпотентность — latent. **Р3:** Чисто. Caching extract — теоретическая проблема, Agda codegen pure. | Тест changedSlotsFromCache при scope с 20+ slots. deepEqual performance на 1000-slot model. |
| V3 | List/Nat/Number ops | 391-533 | 140 | 3 | `listToArray` — try/catch добавлен (r29) — но `MAX_LIST_ITEMS = 100000` — это мало или много? Зависит от use case. `arrayToList` — `construct('_∷_', arr[i], result)` — строит список справа налево — О(n) но каждый element создаёт closure — memory. `natToNumber` — `MAX_NAT_VALUE = 100000` — если Nat > 100000, возвращает 100000 с warning — silent data loss. `ensureNumber` — `natToNumber(n)` для function input — а если function не Scott-encoded ℕ? `natToNumber` returns 0 после break — неожиданно **Р1-2:** Чисто. listToArray r29 fix OK. natToNumber fallback 0 deliberate. **Р3:** Чисто. natToNumber fallback 0 deliberate. | Тест listToArray на максимально возможном списке (100000 items) — timing. construct closure memory с 100K arrayToList. |
| V4 | String/Maybe/Bool ops | 535-625 | 90 | 3 | `ensureString` hardened (r29) — `typeof data === 'symbol'` → `data.toString()` — но `Symbol.prototype.toString` может быть monkey-patched? Маловероятно. `fromMaybe` — `matchOr(maybe, {just, nothing}, null)` — если maybe object-format, match handles? `toMaybe` — `value != null` (loose) — catches undefined too — correct? `fromBool` — `matchOr(bool, {true, false}, false)` — а если bool Scott-encoded с другими constructor names? `toBool` — `value ? construct('true') : construct('false')` — а если value BigInt 0n? `0n` falsy → 'false'. Correct. **Р1-2:** Чисто. fromBool/fromMaybe constructor names lowercase — match Agda. **Р3:** Чисто. toMaybe(0) → just(0) correct. | Тест matchOr с mixed Scott-encoding formats (function vs object). ensureString на circular object. |

### reactive-gl.js (5,419 LOC)

| # | Тема | Строки | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|--------|-----|---|------------|------------|
| G1 | Scott interpreters | 60-365 | 300 | 3 | `interpretSceneNode` — все конструкторы (mesh, group, icon, light, bindTransform, bindMaterial, text3D, bindText3D, bindLight, bindChildren, animate, interactiveGroup, named, instanced, bindInstanced, batched) — есть ли пропущенные? Если Agda добавит новый конструктор, JS бросит при вызове. `interpretGeometry` — plain object check `geom.type` — откуда берутся plain objects? Из Geometry.Primitives (roundedBox и др.) — правильный ли маппинг? `interpretMaterial` — textured: `url: texHandle` — texHandle это строка URL? Или Scott-encoded? **Р1-2:** Чисто. Все SceneNode конструкторы покрыты. emptyGeometry handler добавлен. **Р3:** Чисто. Все SceneNode конструкторы покрыты. emptyGeometry handler добавлен. | Тест interpretSceneNode с unknown конструктором. CSG types в getOrCreateGeometry (csgUnion etc. — нет handler). |
| G2 | GLSL шейдеры | 369-825 | 460 | 3 | Phong frag — `for (int i = 0; i < 8; i++) { if (i >= u_numLights) break; }` — loop unrolling в GLSL — все ли GPU поддерживают dynamic break? Formально да по ES 3.0 spec. PBR frag — `pow(1.0 - cosTheta, 5.0)` — а если cosTheta > 1.0 из-за precision? `clamp` нет. Instanced vert — `mat3(a_instanceMatrix)` как approximation для normal matrix — неправильно при non-uniform scale (documented approximation). Pick frag — `u_objectId` float precision — 24-bit ID в 8-bit channels — есть ли precision loss при float→byte conversion? Text frag — `alpha < 0.1` threshold — hard-coded, не configurable **Р1-2:** Чисто. GLSL ES 3.0 dynamic break OK. **Р3:** Чисто. PBR cosTheta clamped через max(dot,0). | Тест instanced vert normal matrix при non-uniform scale. Text alpha threshold 0.1 — configurable? |
| G3 | Матричная математика | 830-1030 | 200 | 3 | `mat4Perspective` — `fov` в radians или degrees? Caller responsibility. `mat4LookAt` — `up` parallel to view direction → `len = 0` → `ix = 0` → NaN в результате. `mat4Invert` — `det < 1e-10` threshold — может быть слишком строгий для далёких объектов? `mat4Multiply` — O(64) operations — hot path? Может ли быть bottleneck для scene с 1000 объектов? `unprojectPoint` — `ww < 1e-10` — а если ww отрицательный (за камерой)? **Р1-2:** Чисто. **Р3:** **Исправлено**: mat4LookAt parallel up → alternative up vector fallback. | Тест mat4Invert precision при очень далёких объектах (det ≈ 1e-10). mat4Multiply performance на 1000 объектов. |
| G4 | Ray intersection | 1031-1290 | 260 | 3 | `raySphere` — `a = dir.x² + dir.y² + dir.z²` — если dir normalized, a ≈ 1.0 — но нигде не гарантируется нормализация. `rayAABB` — `Math.abs(d) < 1e-10` — а если луч почти параллелен грани? `tmin > tmax` check может пропустить edge case. `rayPlane` — `ray.dir.y` only — а если плоскость не XZ (повёрнутая)? Нужен transform в local space. `rayToLocal` — `localRay.scale` — используется для конвертации t обратно в world — а если scale неравномерный по осям? `rayIntersectEntry` — cylinder approximated как box — пропускает hits по скруглённым краям. `rayPickEntries` — linear scan O(n) — может быть медленно для >1000 объектов. `rayPickGroup` — recursive без ограничения глубины **Р1-2:** Чисто. Нормализация dir не требуется (raySphere вычисляет a). **Р3:** Чисто. rayAABB slab method correct for parallel rays. | Тест rayPickEntries performance при 5000 объектов — O(n) scan. rayPickGroup глубина 100+ — stack. |
| G5 | Базовая геометрия + upload | 1291-1460 | 170 | 3 | `createBoxGeometry` — normals hard-coded — правильные ли для каждой грани? UV mapping — каждая грань 0-1 — а если нужен atlas? `createSphereGeometry` — `slices=24, stacks=16` — достаточно для гладкой сферы? При radius < 0.01 degenerate? `createPlaneGeometry` — UV tiled по size — `size.x, size.y` как tile repeat — а если size < 0? `uploadGeometry` — `_indexType` stored — но `drawElements` at call site checks string equality — typo risk **Р1-2:** Чисто. **Р3:** Чисто. Sphere radius<0.001 degenerate but no crash. | Тест createPlaneGeometry с size < 0. uploadGeometry с 0 vertices. |
| G6 | Extended geometry | 1460-2250 | 790 | 3 | `createTorusGeometry` — normals `(cosV*cosU, sinV, cosV*sinU)` — правильно ли? Должен указывать от центра tube к поверхности. `createCapsuleGeometry` — `halfHeight = height * 0.5 - radius` — а если height < 2*radius? Negative halfHeight → inverted caps. `createConeGeometry` — normals для sloped surface: `len = sqrt(1 + slope²)` — а если slope = 0 (flat)? `createPyramidGeometry` — face normal cross product — winding order consistent? `createTubeGeometry` — inner wall reversed winding — correct? `createRingGeometry` — flat ring, normals все (0,1,0) — а если ring не в XZ plane? `createTetrahedronGeometry` — vertex coords `a = radius/sqrt(3)` — правильная формула? `createRoundedBoxGeometry` — SDF solver с binary search fallback — сходимость за 50 итераций? Edge cases: radius > min(dims)/2? **Р1-2:** **Исправлено ранее**: capsule halfHeight Math.max(0). **Р3:** Чисто. Capsule clamp OK. | Тест createConeGeometry slope=0 (flat top). createPyramidGeometry sides=2 degenerate. createTorusGeometry minor>major. |
| G7 | Текстуры + шрифты + текст | 2250-2700 | 450 | 3 | `createTextureCache` — LRU eviction `cache.delete(oldest); cache.set(url, entry)` — Map insertion order. `img.crossOrigin = 'anonymous'` — а если сервер не шлёт CORS headers? Ошибка тихая (errorTex). `createFontAtlas` — `atlasSize = 1024` fixed — а если шрифт очень большой или символов много? `if (curY + h > atlasSize) break` — символы после break не в atlas → fallback `glyphs.get('?')`. `buildTextGeometry` — `Math.max(0, ...lines.map(l => l.width))` — spread на большом массиве → stack overflow? `createTextVAO` — dummy normals `(0,0,1)` — а если текст повёрнут? Освещение неправильное **Р1-2:** Чисто. **Р3:** Чисто. FontAtlas overflow — graceful (skip + fallback '?'). | Тест buildTextGeometry с 50K символов — spread stack limit. createTextVAO при повёрнутом тексте — normals. |
| G8 | Shader compilation + programs + pick FB | 2700-2970 | 270 | 3 | `compileShader` — `gl.getShaderInfoLog` — может вернуть null? `linkAndCleanShaders` — detach+delete shaders after link — correct GL lifecycle. `createPickFramebuffer` — `width <= 0 || height <= 0` → null — все caller проверяют null? `createPhongProgram` — light uniform arrays hardcoded `i < 8` — matches CONFIG.MAX_LIGHTS? `geometryKey` — cache key as string concatenation — collision risk если dims содержат запятую? **Р1-2:** Чисто. **Р3:** Чисто. | Тест createPhongProgram MAX_LIGHTS consistency (8 in JS vs shader). compileShader getShaderInfoLog null handling. |
| G9 | Geometry cache + easing + lerp/slerp | 2975-3250 | 275 | 3 | `getOrCreateGeometry` — `GEO_CACHE_MAX = 256` — eviction стратегия? Или просто растёт? `makeCubicBezier` — Newton-Raphson solver — сходимость для extreme control points? `resolveEasing` — `easingFunctions[easing]` — а если easing неизвестная строка? Returns undefined → NaN. `slerpQuat` — `dot < 0` → negate b — shortest path. Но: `Math.acos(dot)` — если dot > 1 из-за precision → NaN. Нужен clamp. `lerpTransform` — независимая интерполяция pos/rot/scale — корректно для большинства случаев **Р1-2:** **Исправлено**: slerpQuat Math.acos clamp Math.min(1, dot). **Р3:** Чисто. resolveEasing unknown → linear fallback. slerp clamp OK. | Тест makeCubicBezier с extreme control points (x1=0, y1=100). GEO_CACHE_MAX=256 eviction order. |
| G10 | Instance buffers + collect | 3250-3810 | 560 | 3 | `buildInstanceMatrices` — `Float32Array(count * 16)` — если count = 0? Пустой массив OK. `transformToMat4` — delegates to `mat4FromTRS` — проверить что transform всегда имеет pos/rot/scale. `setupInstanceAttributes` — location 2-5 для mat4 — конфликт с UV attribute (location 2)? Нет: instanced shader не использует UV. `collectBindings` — recursive traversal — stack overflow на глубокой сцене? `collectNode` — `overrideTransform` — используется для animate — а если и parent и child имеют transform? `registerPickable` — `pickRegistry.nextId++` без bounds check — overflow at 2^24. `pickIdToRGB` — `((id >> 16) & 0xFF)` — 24-bit max **Р1-2:** Чисто. pickRegistry 24-bit overflow теоретический. **Р3:** Чисто. count=0 OK. | Тест overrideTransform при parent+child оба с animate. collectBindings memory при 10000 nodes. |
| G11 | updateGLBindings + tick | 3836-4150 | 310 | 3 | `updateGLBindings` — `transformEqual`/`materialEqual` — shallow comparison — если nested object refs change but values same? `tickSingleAnimation` — `elapsed / durationMs` — если durationMs = 0? Division → Infinity → clamp to 1.0? Нет clamp — easing(Infinity). `tickAnimateNodes` — `dt = timeSeconds - anim.lastTimestamp` — first frame dt = 0 → skip. Second frame dt correct. `anim.accumTime += dt` — unbounded growth — для hover-only animations, runs forever while hovered **Р1-2:** **Исправлено**: tickSingleAnimation durationMs=0 guard. **Р3:** Чисто. durationMs=0 guard OK. | Тест accumTime при hover-only animation 10 минут. materialEqual false positive при same-value different-ref objects. |
| G12 | uploadLights + renderEntry + renderScene | 4154-4490 | 340 | 3 | `uploadLights` — `Math.min(lights.length, 8)` — lights > 8 silently dropped. `renderEntry` text — blending removed from text rendering (r25) — а если text rendered in opaque pass? Line 4459 always puts text in transparent. `isTransparent` — checks only mesh and group — instanced, text3D, icon not checked. `entryDepth` — uses transform.pos only — а если entry type has no transform? Returns (0,0,0) distance. `renderScene` — `gl.clearColor(0.1, 0.1, 0.1, 1.0)` hardcoded — not configurable **Р1-2:** Чисто. lights > 8 clamped. clearColor hardcoded. **Р3:** **Исправлено**: isTransparent теперь проверяет instanced. | Тест entryDepth при entry без transform. clearColor configurability (currently hardcoded). |
| G13 | Pick-pass rendering | 4490-4640 | 150 | 3 | `renderPickEntry` — recursive for groups — depth limit? `renderPickBuffer` — `gl.enable(gl.DEPTH_TEST)` — а `gl.depthMask(true)`? Если предыдущий прогон оставил `depthMask(false)` — picking сломан. (renderScene восстанавливает, но edge case). `readPickId` — `pickFB.height - 1 - y` — off-by-one если height = 0? pickFB null-guard exists. `rgbToPickId` — `r | (g << 8) | (b << 16)` — а если r/g/b > 255? Impossible from readPixels UNSIGNED_BYTE **Р1-2:** Чисто. **Р3:** Чисто. depthMask restored by renderScene. | Тест renderPickEntry depth > 30. readPickId при pickFB height=1 (off-by-one). |
| G14 | initGLCanvas + events + resize | 4640-5420 | 780 | 3 | `initGLCanvas` — `canvas.getContext('webgl2')` — fallback если WebGL2 недоступен? Нет — бросает. Drag state machine: mousedown → mousemove → mouseup — а если mouseup вне canvas? `pointerup` на document? `setPointerCapture`? HiDPI resize — `canvas.width = drawW; canvas.height = drawH` — а если drawW/drawH = 0 (element hidden)? GL error. `ResizeObserver` — callback может вызваться при каждом frame если element анимируется — throttle? MutationObserver — `document.body` subtree — может быть тяжело для DOM с 10K нод. `disposeGLCanvas` — все ли GL ресурсы freed? Programs, geometries, textures, FBOs, VBOs. Focus: `tabIndex = 0` — а если canvas уже имеет tabIndex? Keyboard: `onKeyDown` в focused canvas — а если canvas теряет focus между keydown и keyup? **Р1-2:** Чисто. WebGL2 unavailable бросает. Drag/mouseup/resize/dispose — всё корректно. **Р3:** Чисто. ResizeObserver CSS lock prevents loop. | Тест disposeGLCanvas — all GL resources freed? Programs, textures, FBOs. Context loss/restore cycle. |

### Остальной JS (5 тем)

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| O1 | Worker pool | worker-pool.js | 224 | 3 | `submit` — `activeWorker` closure variable — а если onmessage и onerror вызываются оба? (network error + partial response). `_processQueue` — `while` loop — если tryRun вызывает enqueue → infinite loop? `_cleanup` — `Date.now() - this.lastUsed > POOL_IDLE_TIMEOUT` — а если clock jumps backward (NTP sync)? `destroy` — `entry.task.onError('Pool destroyed')` в try/catch — если onError бросает, catch проглатывает **Р1-2:** **Исправлено**: handled flag предотвращает double-fire onmessage+onerror. **Р3:** Чисто. handled flag OK. No infinite loop. | Тест pool maxSize enforcement при rapid submit. destroy mid-computation graceful cleanup. |
| O2 | Buffer registry | buffer-registry.js | 270 | 3 | `allocate` — `SharedArrayBuffer(size)` — если size = 0? SAB(0) валиден? `nextHandle` — overflow при `Number.MAX_SAFE_INTEGER`? Практически нет. `getView` — switch без default return — last line `default: return new Uint8Array(buffer)` есть. `copyImageData` — `imageData.data.length === view.length` — а если buffer resized между allocate и copy? `getImageData` — `entry.buffer.slice(0)` — на SAB дорого — копирует весь буфер **Р1-2:** Чисто. SAB(0) валиден. **Р3:** Чисто. | Тест copyImageData с mismatched buffer/view lengths. allocate size=Number.MAX_SAFE_INTEGER. |
| O3 | SVG events | svg-events.js | 196 | 3 | `screenToSvg` — `ctm.inverse()` — может бросить если CTM singular (zero-sized SVG). `findSvgRoot` — `tagName === 'svg' || tagName === 'SVG'` — а в namespace? `element.tagName` в HTML lowercase, в XML uppercase — покрыты оба? `createSvgDrag` — `target.setPointerCapture(e.pointerId)` — может бросить если pointer уже captured. `onPointerUp` — `target.releasePointerCapture(e.pointerId)` — если capture уже released → exception. `createSvgPinch` — `initialDistance > 1e-6` — а если distance exactly 0? Division в getCenter safe (just midpoint) **Р1-2:** Чисто. CTM singular в try/catch. **Р3:** Чисто. CTM try/catch, isDragging guard. | Тест createSvgPinch initial distance=0. findSvgRoot в shadow DOM. |
| O4 | Agda RTS primitives | agda-rts.js | 372 | 3 | `uprimIntegerQuot` — `x / y` BigInt division truncates toward zero — correct for quot. Но: `y === 0n` → throws `RangeError`. Нет guard. `_primFloatDecode` — loop `while (!Number.isInteger(mantissa))` — для subnormals может быть очень много итераций (1074 max для float64). Performance? `primFloatToWord64` — DataView approach correct for bit reinterpretation. `uprimFloatEncode` — `mantissa * (2 ** exponent)` — 2^1024 = Infinity, returns Infinity not null. `primNatMinus` — `x - y < 0n → 0n` — correct для natural subtraction. `primFaceForall` — `f(true) == true` — loose equality, но operands both bool, so OK **Р1-2:** Чисто. quot div/0 matches Haskell. float decode ~1074 iterations max. **Р3:** Чисто. quot div/0 = Haskell semantics. | Тест primShowFloat precision на extreme values. primFloatToWord64 roundtrip для subnormals. |
| O5 | Update worker + reactive-auto | update-worker.js + reactive-auto.js | 80 | 3 | `update-worker.js` — `import(modulePath)` — path relative to worker location — correct? `updateFn = NodeModule.ReactiveApp.update(appRecord)` — а если appRecord не имеет update? `reactive-auto.js` — `document.querySelectorAll('[data-agdelte]')` — runs at script load time — а если DOM ещё не ready? **Р1-2:** Чисто. ES module deferred — DOM ready OK. **Р3:** Чисто. ES module deferred. | Тест update-worker.js с module that throws on import. reactive-auto.js с 0 [data-agdelte] elements. |

---

## 2. Haskell Agent Server (1,476 LOC)

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| H1 | AgentServer | hs/Agdelte/AgentServer.hs | 503 | 3 | STM транзакции: атомарность read-modify-write для agent state? Race между HTTP POST и WebSocket push. Exception handling: что если agent step бросает — recovery или crash? WebSocket broadcast: если один client медленный, блокирует ли остальных? Memory: растёт ли state unbounded? Agent garbage collection? Route parsing: path traversal через `..`? Content-Type validation на POST body? **Р1-2:** **Исправлено ранее**: nonce (n+1→n), Content-Type. STM корректен. Race HTTP/WS — by contract. **Р3:** Чисто. STM, path safe, bounded broadcast. Concurrent тесты добавлены. | Тест path traversal с "/../" в URL. Broadcast backpressure при 100 slow clients. Agent hot-swap. |
| H2 | Process | hs/Agdelte/Process.hs | 328 | 3 | Agent execution model: pure coalgebra step — а если step diverges (infinite loop)? Timeout? State snapshots: deep copy или reference? Mutable state через IORef/TVar — consistency при concurrent access? Agent lifecycle: create/destroy/restart — resource cleanup? **Р1-2:** **Исправлено**: timeout 30s на handler. Gate MVar OK. **Р3:** **Исправлено**: timeout 30s. Тесты timeout recovery добавлены. | Тест client reconnect после timeout. IPC socket file cleanup при crash. |
| H3 | Http | hs/Agdelte/Http.hs | 150 | 3 | Request parsing: malformed JSON body? Oversized body? Missing Content-Type header? Response: correct status codes? CORS headers? Error response format consistency? Path routing: case sensitivity? Trailing slash? Query params? **Р1-2:** Чисто. CORS в AgentServer. Body 16MB limit OK. COOP/COEP headers. **Р3:** Чисто. | Тест request с Content-Length mismatch. Response streaming для больших ответов. |
| H4 | STM | hs/Agdelte/STM.hs | 101 | 3 | `retry` semantics: может ли STM transaction retry бесконечно? Starvation? TVar: memory leak если transaction allocates then retries? Composability: nested transactions correct? Exception in transaction: rollback complete? **Р1-2:** Чисто. STM минимальный и корректный. **Р3:** Чисто. | Тест STM transaction с exception — verify rollback. Memory под нагрузкой (1000 concurrent TVars). |
| H5 | WebSocket | hs/Agdelte/WebSocket.hs | 77 | 3 | Connection lifecycle: open → message → close — все states handled? Ping/pong: keepalive timeout? Client disconnect без close frame — detection? Concurrent send: thread-safe? Broadcast: if N clients, O(N) sends — blocking? Message size limit? Binary vs text frames? **Р1-2:** Чисто. **Р3:** Чисто. | Тест ping/pong keepalive timeout. WebSocket binary frame handling. Message size > 1MB. |
| H6 | Server tests | hs/test/Spec.hs | 317 | 3 | Coverage: все ли HTTP endpoints тестируются? WebSocket scenarios? Concurrent access? Error cases? Agent lifecycle? Тесты проходят? Не flaky? **Р1-2:** Чисто. Покрытие проверено. **Р3:** **Исправлено**: 7 тестов добавлены (3 timeout + 4 concurrent). | Тест flaky detection (запуск 10 раз). Test coverage measurement. WebSocket lifecycle тесты. |

---

## 3. Agda Core Library (35,144 LOC)

Специфика: type system ловит type errors, но не логические ошибки. Искать: семантические баги, FFI mismatches, неоптимальный generated JS.

### Core Framework

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| A1 | Event | src/Agdelte/Core/Event.agda | 565 | 3 | Event datatype: все ли конструкторы имеют корректную Scott-encoding семантику? Event↔Sub split: конструкторы в Event vs Sub — правильное ли разделение? Каждый Sub constructor должен быть leaf (без рекурсивного Event). `merge` ассоциативность: `merge (merge a b) c ≡ merge a (merge b c)` — в runtime порядок подписки может отличаться. `foldE` — начальное состояние и step function — типы совпадают с runtime interpretEvent? `mapFilterE` — Maybe return — nothing = filter out — runtime проверяет `isJust`? `switchE` — метa-event returns new Event — тип рекурсивный? **Р1-2:** 32 Event/Sub конструктора = 1:1 match с JS. allocShared arity mismatch исправлен. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Интеграционный тест: runtime interpretEvent для switchE с рекурсивным meta-event. Edge case: merge пустого Event с non-empty. |
| A2 | Signal | src/Agdelte/Core/Signal.agda | 180 | 3 | Signal абстракция: continuous vs discrete? Memory semantics — удерживает ли Signal ссылку на все предыдущие значения? `accum` — accumulator leak. `map`, `filter`, `merge` — правильная ли семантика для одновременных events? **Р1-2:** Чисто. Signal memory-safe (coinductive, no past accumulation). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест scanS с высокочастотным input — проверить отсутствие memory leak. Edge case: pre на initial value. |
| A3 | Task | src/Agdelte/Core/Task.agda | 181 | 3 | Task монада: `pure`, `fail`, `httpGet`, `httpPost`, `>>=` — все ли правильно компилируются в Scott-encoding? Error propagation: `fail` должен прерывать цепочку — runtime executeTask проверяет? `httpPost` body type — String? А если нужен binary? **Р1-2:** **Исправлено**: ok/er rename в alwaysThen. Все 7 функций проверены. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Runtime integration: >>= chain из 5+ Task с ошибкой в середине — проверить propagation до конца. httpPost с binary body через Buffer. |> side-effect preservation. |
| A4 | Cmd | src/Agdelte/Core/Cmd.agda | 153 | 3 | `_<>_` — моноидальная композиция — ассоциативна ли в runtime? `ε` — identity. Полнота: все ли нужные side effects покрыты (focus, blur, scroll, clipboard, localStorage, routing, WS, workers, buffers)? Нет ли дубликатов? `delay` — тип ms: ℕ? А если нужен Float delay? **Р1-2:** 23 Cmd конструктора = 1:1 match с executeCmd. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест _<>_ с 10+ Cmd в цепочке — порядок side effects в runtime. Edge case: delay 0 vs delay 1. |
| A5 | Result | src/Agdelte/Core/Result.agda | 64 | 3 | `ok`/`err` конструкторы — правильный ли маппинг на JS `cb['ok'](value)` / `cb['err'](error)`? Result.Event — event that dispatches Result — composition с Task? **Р1-2:** ok/err Scott-encoding корректен. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Runtime тест: Result.Event compose с Task attempt — err path через полный цикл update→cmd→result. |
| A6 | App | src/Agdelte/App.agda | ~100 | 3 | `ReactiveApp` record: `init`, `update`, `template`, `cmd`, `subs` — все ли optional? Что если `cmd` = nothing? `subs` = nothing? Runtime checks `if (!subsFunc) return`. `template` — type: `Node Model Msg` — right? **Р1-2:** **Исправлено**: cmd PRE-update comment. Optional fields (cmd/subs) runtime null handling OK. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Runtime тест: ReactiveApp с cmd=nothing + subs=nothing одновременно. Hot-reload сценарий при смене template. |

### Reactive System

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| A7 | Node | src/Agdelte/Reactive/Node.agda | 421 | 3 | Все Node конструкторы: `text`, `bind`, `elem`, `empty`, `when`, `whenT`, `foreach`, `foreachKeyed`, `scope`, `scopeProj`, `zoomRT`, `glCanvas` — каждый должен match с handler в reactive.js `renderNode`. Binding type: `extract : Model → String` — а если нужен non-string binding (числовой атрибут)? Attr type: `attr`, `attrBind`, `on`, `onValue`, `onValueScreen`, `onKeyFiltered`, `style`, `styleBind` — match с `applyAttr`. `Transition` record fields — match с `NodeModule.Transition.*` extractors в JS **Р1-2:** 13 Node конструкторов match renderNode. Transition fields match. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест whenT/foreach/foreachKeyed взаимодействие с bind внутри scope. Non-string attrBind (числовой) через runtime. |
| A8 | Diagram | src/Agdelte/Reactive/Diagram.agda | 253 | 3 | Diagram composition operators — правильная семантика? Diagram → Node conversion — полная ли? **Р1-2:** Чисто. Diagram server-side only, не Node. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Diagram ⊕D merge с конфликтными slot names — проверить приоритет и warning на server-side. |
| A9 | Optic | src/Agdelte/Reactive/Optic.agda | 183 | 3 | Lens laws: get-set, set-get, set-set — доказаны или postulated? `zoomRT` optic — get/wrap pair — consistent? **Р1-2:** Чисто. Все 4 composition operators корректны. zoomRT composition OK. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Prism composition: chain из 3+ prism с Nothing в середине. zoomRT тройная вложенность через runtime. |
| A10 | Inspector + TimeTravel + BigLens | Inspector+TimeTravel+BigLens.agda | 422 | 3 | BigLens protocol: peek → JSON serialize model → WS send. Всё ли сериализуемо? Over: JSON → message → dispatch — type safety потеряна на JSON boundary. TimeTravel: history depth — configurable? **Р1-2:** **Исправлено ранее**: redo trim. BigLens string-serialized, model-agnostic. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест TimeTravel replay при maxSize=1 (single-step history). BigLens с model содержащим function — serialize fallback. |
| A11 | Lens + ProcessOptic + RemoteOptic | Lens+ProcessOptic+RemoteOptic.agda | 286 | 3 | Composition chains — правильный ли порядок get/set? Remote sync — eventual consistency или strong? Conflict resolution? **Р1-2:** Чисто. Composition chains корректны на всех уровнях. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест RemoteOptic roundtrip с emoji/surrogate pairs. ProcessOptic reconnect после timeout — state recovery. |

### Concurrent & Agent System

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| A12 | Agent | src/Agdelte/Concurrent/Agent.agda | 48 | 3 | Coalgebra interface: `step : Input → State → State`, `observe : State → Output` — минимальный ли набор? Нет ли missing operations (init, destroy)? **Р1-2:** Чисто. Moore machine semantics. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Формально верифицировать: postulated dual involution для Agent coalgebra. Тест mapObserve∘mapInput identity law. |
| A13 | Session | src/Agdelte/Concurrent/Session.agda | 224 | 3 | Session types: `send`, `recv`, `end`, `choice`, `offer` — полный ли набор? Дуальность: `dual(send A S) = recv A (dual S)` — доказана? Deadlock freedom — гарантируется ли типами? **Р1-2:** Чисто. Duality корректна по инспекции. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Заменить postulated dual involution на constructive proof. Тест deadlock detection при circular choice/offer. |
| A14 | SessionExec | src/Agdelte/Concurrent/SessionExec.agda | 240 | 3 | Execution: session type → runtime behavior. FFI calls для send/recv — правильный ли маппинг? Error handling: что если send fails? Channel закрыт? **Р1-2:** Чисто. Pipeline >>>  sound. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Проверить TERMINATING pragma на coroutineAgent — доказать termination или добавить fuel. Pipeline >>> error recovery. |
| A15 | SharedAgent + DepAgent | SharedAgent+DepAgent.agda | 355 | 3 | Shared state: read/write atomicity? Dependent agent: dependency cycle detection? Resolution order? **Р1-2:** Чисто. Atomicity — caller responsibility (MVar). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест DepAgent cyclic dependency detection (A depends on B depends on A). SharedAgent concurrent step ordering guarantee. |
| A16 | CoSession | src/Agdelte/Concurrent/CoSession.agda | 219 | 3 | Co-session = dual perspective — symmetry с Session.agda? Protocol compatibility check? **Р1-2:** Чисто. Symmetry с Session maintained. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест unroll глубиной N=20 — правильность splice boundaries. Co-session symmetry property test с quickcheck. |
| A17 | SessionForm | src/Agdelte/Concurrent/SessionForm.agda | 173 | 3 | Form fields → session messages — mapping complete? Validation на Agda side vs JS side? **Р1-2:** Чисто. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест formAgent multi-phase wizard с back-step. Удалить wizardProtocol если unused или связать с formAgent. |
| A18 | Obligation | src/Agdelte/Concurrent/Obligation.agda | 159 | 3 | Obligation: must-do before proceed — enforcement mechanism? Compile-time vs runtime? **Р1-2:** Чисто. bracket + tryCatch корректны. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест bracket exception в acquire phase. withTwoIpc: первый close бросает — второй ресурс всё равно закрыт? |
| A19 | Wiring | src/Agdelte/Concurrent/Wiring.agda | 321 | 3 | Agent composition topology — correct routing? Fan-out/fan-in? Error propagation через wiring? Debug module — overhead в production? **Р1-2:** Чисто. loop feedback pre-step observation. ⊕ silent mismatch drop. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест fan-out 1→N→1 fan-in diamond topology. Wiring.Debug overhead benchmark: включён vs выключен. |
| A20 | ProcessOpticLinear | ProcessOpticLinear.agda | 87 | 3 | Linear types — resource must be used exactly once — enforced by Agda? Или convention? **Р1-2:** Чисто. Indexed types enforce linear. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест: indexed types реально блокируют double-close на уровне Agda typechecker. queryAndClose path coverage. |

### FFI Boundary (🔴 высший приоритет)

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| A21 | FFI.Browser | src/Agdelte/FFI/Browser.agda | 154 | 3 | Каждый `postulate` — сигнатура совпадает с JS реализацией? Типы: `ℕ` → BigInt, `Float` → Number, `String` → String, `Bool` → Scott-encoded — все ли маппинги правильные? `Maybe` — JS null или Scott nothing? Callback arity — curried в Agda, curried в JS? Или uncurried? **Р1-2:** **Исправлено ранее**: mkMouseEvent Float. Все postulates верифицированы. IO-typed inconsistency — design choice. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест setTimeout chaining без IO wrapper — порядок callback execution. querySelector на несуществующий selector — Maybe Nothing path. |
| A22 | FFI.Server | src/Agdelte/FFI/Server.agda | 307 | 3 | Haskell FFI: `foreign import` correctness? Type marshalling Agda→Haskell→IO? Blocking calls — правильная ли concurrency model? Exception propagation through FFI boundary? **Р1-2:** Чисто. Все Haskell FFI типы match. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест serveAgentProcess graceful shutdown — добавить IpcServer handle return. Exception propagation через Haskell FFI boundary. |
| A23 | FFI.Shared | src/Agdelte/FFI/Shared.agda | 66 | 3 | Cross-platform: функции используемые и в browser и в server — одинаковая ли семантика? Platform-specific behavior hidden behind shared interface? **Р1-2:** Чисто. SharedBuffer browser-only (no GHC COMPILE). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Добавить GHC COMPILE pragma для SharedBuffer или explicit browser-only guard. Тест cross-platform semantics parity. |

### HTML Controls

| # | Тема | Файлы | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|-------|-----|---|------------|------------|
| A24 | DatePicker | Html/Controls/DatePicker.agda | 722 | 3 | Календарная логика: дней в месяце (leap year!), первый день недели (locale?), переход month→month, year→year. Edge: 2024-02-29. Timezone: Date в JS — local timezone, а DatePicker показывает какой? Selection: range selection, disabled dates. Generated HTML: accessibility (ARIA), keyboard navigation **Р1-2:** **Исправлено**: firstDayOfMonth перенесён перед datePickerSimple + renderEmptyDays. dateRangePicker наследует фикс. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест focus trap: Tab cycle внутри DatePicker overlay. Escape закрывает picker. ARIA-expanded state sync. |
| A25 | Slider | Html/Controls/Slider.agda | 446 | 3 | Min > max — handled? Step = 0 — division by zero? Drag precision: float rounding при step. Vertical slider? Range slider (two thumbs)? Touch events? Accessibility: aria-valuenow, aria-valuemin, aria-valuemax **Р1-2:** **Исправлено**: orient→writing-mode + aria-orientation. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест indeterminate checkbox visual state. Vertical slider touch event на mobile. Validation: min > max graceful. |
| A26 | Dropdown + Popover + Modal | Dropdown+Popover+Modal.agda | 588 | 3 | Dropdown: click-outside close — event handler cleanup? Search/filter в dropdown? Popover: positioning — overflow viewport? Focus trap в modal — tab cycle. Escape key — все ли закрывают? Nested modals? Z-index stacking? **Р1-2:** Чисто (design gaps в backlog). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест focus trap Tab cycle в Modal. Escape dismiss для nested Dropdown внутри Popover. ARIA-expanded linkage. |
| A27 | Accordion + TabBar + Sidebar | Accordion+TabBar+Sidebar.agda | 488 | 3 | Accordion: multiple open vs single? Animation height transition. TabBar: active tab state sync. Sidebar: responsive collapse. Keyboard navigation: arrow keys, home/end? **Р1-2:** Чисто (design gaps в backlog). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Accordion whenT миграция — conditional panel render. TabBar aria-controls ID match с panel. |
| A28 | Table + DataGrid | Table+DataGrid.agda | 300 | 3 | Sorting: stable sort? Multi-column? Pagination: off-by-one в page count? Empty data display. Column resize: min width? DataGrid: virtualization для больших datasets? **Р1-2:** **Исправлено**: Pagination empty-state display. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест numberedPagination edge: totalPages=1000. DataGrid virtual scroll с indeterminate checkbox column. |
| A29 | TreeView + Breadcrumb | TreeView+Breadcrumb.agda | 330 | 3 | TreeView: expand/collapse state. Lazy loading children? Drag-drop reorder? Breadcrumb: path sync с model. Truncation при overflow? **Р1-2:** **Исправлено**: TreeView CSS margin-left удалён (double indent fix). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест TreeView expand-all при depth=15 — DOM performance. Breadcrumb truncation с ellipsis при >7 items. |
| A30 | Toast + Tooltip + Spinner + Skeleton | Toast+Tooltip+Spinner+Skeleton.agda | 640 | 3 | Toast: auto-dismiss timer — cleanup при unmount? Stacking multiple toasts? Tooltip: delay before show? Position flip при viewport edge? Spinner: accessible `role="status"`? Skeleton: layout shift при content load? **Р1-2:** **Исправлено**: Spinner role="status" + sr text ×11 вариантов. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Toast stacking: 5 toasts → auto-dismiss порядок FIFO. overlaySpinner aria-busy на parent container. |
| A31 | Checkbox + Radio + Stepper + Progress + Pagination | CheckboxRadioStepperProgressPagination.agda | 795 | 3 | Checkbox: indeterminate state? RadioGroup: none selected allowed? Stepper: bounds enforcement min/max. Progress: value > max? Negative value? Pagination: total = 0 → 0 pages or 1? **Р1-2:** **Исправлено**: Progress CSS border-radius var. Pagination OK. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Checkbox indeterminate→checked→unchecked cycle. Progress с value=NaN — defensive clamp. Stepper min=max. |

### SVG System

| # | Тема | Файлы | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|-------|-----|---|------------|------------|
| A32 | SVG core | Svg/Elements+Attributes+Events+Transform.agda | 711 | 3 | Element names: typos в SVG element strings? Attribute names: camelCase vs kebab-case (SVG uses kebab). Event handler attributes vs addEventListener — which approach? Transform: matrix composition order? **Р1-2:** Чисто. SVG element/attribute/event naming всё корректно. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест SVG Transform matrix composition: rotate then scale vs scale then rotate — order matters. xlink:href deprecation. |
| A33 | SVG ViewBox + Accessibility + Filter + Gradient | ViewBox+Accessibility+Filter+Gradient.agda | 486 | 3 | viewBox: `min-x min-y width height` — negative width/height? preserveAspectRatio? Accessibility: role, aria-label — на каких SVG elements? Filter: filter URL reference — `url(#id)` — id uniqueness? Gradient: stop offset 0-1 — clamped? **Р1-2:** **Исправлено**: dropShadowFilter input, glowFilter color, accessibleSvg unique IDs. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Gradient stop offset=1.5 — browser clamp vs Agda validation. dropShadow filter chain visual regression. |
| A34 | SVG Smil + Path + Morph | Smil+Path+Morph.agda | 667 | 3 | SMIL: begin/dur/end attributes — string format correct? Path: `M`, `L`, `C`, `A` commands — float precision in generated strings. Morph: path interpolation — same number of commands required? Different command types? **Р1-2:** **Исправлено**: accumLength control points, Smil sync offset, idAttrs helper. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Morph lerpPath с разным количеством команд. pathLength accumulation accuracy на S-curve. |
| A35 | SVG Controls: Button, Tooltip, Progress, Slider | Svg/Controls/core.agda | 1212 | 3 | SVG Button: click hit area = visual area? SVG Tooltip: text positioning. SVG Progress: arc calculation (2π * progress). SVG Slider: drag → value mapping — linear? Logarithmic? **Р1-2:** **Исправлено**: SVG Slider onChange wired через onSvgClick. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест SVG Slider click→value при len=0 (division by zero guard). Progress arc calculation при value=0 и value=1. |
| A36 | SVG Controls: interactive | Svg/Controls/interactive.agda | 1175 | 3 | Draggable: bounds enforcement? Snapping? Checkbox SVG: check/uncheck animation. RadioGroup: group constraint. Toggle: on/off transition. Knob: circular drag → angle → value — atan2 correct? **Р1-2:** **Исправлено**: event bubbling removed (Checkbox/Radio/Toggle labeled). **Исправлено**: Knob Taylor normalize. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Knob atan2 accuracy при углах близких к ±180°. Draggable snap-to-grid с fractional step. |
| A37 | SVG Controls: display | Svg/Controls/display.agda | 1013 | 3 | Legend: item overflow? Connector: path routing (straight, bezier, orthogonal). Axis: tick count algorithm — nice numbers? Log scale? Gauge: needle rotation math. RangeSlider: min thumb can't exceed max thumb? **Р1-2:** **Исправлено**: Axis div/0, Connector arrows + curved + dash, RangeSlider negative width clamp. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Axis nice-numbers для log scale input. Connector orthogonal routing с 3+ waypoints. |
| A38 | SVG Gauges | Svg/Controls/Gauges/*.agda | 1022 | 3 | Gauge: arc from angle to angle — direction (CW/CCW)? Value clamp to min/max? ProgressRing: stroke-dasharray calculation. Sparkline: data normalization — min=max? Zero data points? **Р1-2:** **Исправлено**: Taylor sin/cos normalize (Gauge+ProgressRing), semicircle sweep, sparkline empty, gauge textColor + labelColor configurable. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест fullGauge squarified layout при extreme aspect ratio. ProgressRing multiSegment с пустым сегментом. |
| A39 | SVG Charts: basic | Charts/Line+Area+Bar+Scatter.agda | 1054 | 3 | Axis scaling: auto range from data min/max — zero included? Negative values? Log scale? Bar chart: bar width calculation — overlapping bars? Stacked? Scatter: point overlap? Jitter? Line: missing data points — gap or interpolate? **Р1-2:** **Исправлено**: Bar negative values (absFloat+baseline), Area stacked reversed. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Line chart zero-anchoring при all-negative data. Bar chart gap > barWidth — negative bar width guard. |
| A40 | SVG Charts: advanced | Charts/Pie+Radar+Timeline+Heatmap.agda | 983 | 3 | Pie: slices sum > 100%? Sum = 0? Single slice = full circle? Labels overlap? Radar: polygon area — concave for negative? Timeline: overlapping events? Zoom/pan? Heatmap: color scale — linear? Discrete? Missing cells? **Р1-2:** **Исправлено**: Pie single-slice circle, Radar clamp + axisMax guard, Heatmap div/0 + global numCols. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест multi-column Sankey с circular flow detection. Flat timeline overlapping events — collision resolution. |
| A41 | SVG Charts: complex | Charts/Network+OrgChart+Sankey+Treemap+Flowchart.agda | 1617 | 3 | Network: force layout converging? Node overlap? Edge crossing minimization? OrgChart: tree layout — wide trees overflow? Sankey: flow conservation (sum inputs = sum outputs per node)? Treemap: squarified algorithm — degenerate cases? Flowchart: edge routing — overlap with nodes? **Р1-2:** **Исправлено**: Sankey link offset/scaling/dedup, Treemap dim guard, Network arrow offset. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Treemap squarified algorithm при 1 item и при 100+ items. Flowchart orthogonal edge routing overlap. |

### CSS System

| # | Тема | Файлы | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|-------|-----|---|------------|------------|
| A42 | CSS core | Css/core.agda | 816 | 3 | Property names: typos? Vendor prefixes needed? Unit generation: `px`, `em`, `rem`, `%`, `vh`, `vw` — all handled? Color: hex, rgb, rgba, hsl, hsla — string generation correct? Easing: `cubic-bezier(x1,y1,x2,y2)` — string format? Animate: @keyframes generation — unique names? **Р1-2:** Чисто. CSS core property names/units корректны. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест generated CSS: dark theme variable override completeness. cubicBezier x1/x2 outside [0,1] validation. |
| A43 | CSS Controls | Css/Controls/*.agda | 1400 | 3 | Class naming: consistent convention (BEM? Flat?)? CSS variable names: `--agdelte-*` prefix consistent? Responsive breakpoints: consistent across controls? Hover/focus/active states — all controls? Dark theme support via variables? **Р1-2:** **Исправлено**: :focus-visible ×12 файлов, border-radius var. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест tablet breakpoint (768px) consistency across all 12 control CSS files. Dark theme :focus-visible contrast ratio ≥ 4.5. |

### WebGL System

| # | Тема | Файлы | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|-------|-----|---|------------|------------|
| A44 | WebGL Types + Builder | WebGL/Types+Builder.agda | 413 | 3 | Scene type: all node types covered? Camera: perspective + orthographic — missing types? Material: unlit, flat, phong, pbr, textured — complete? Light: ambient, directional, point, spot — complete? Transform: position, rotation (quaternion), scale — Euler angles support? **Р1-2:** Чисто. FFI issues в backlog (Culling/Batching Scott-encoding). **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест SceneNode Scott-encoding destructure через FFI boundary. emptyGeometry handler — runtime crash guard. |
| A45 | WebGL Geometry | WebGL/Builder/Geometry/*.agda | 636 | 3 | Primitives: box, sphere, plane, cylinder + extended (roundedBox, torus, capsule, cone, pyramid, tube, ring, tetrahedron, octahedron, icosahedron, dodecahedron) — parameter types correct? Degenerate params handled? Procedural: параметрическая генерация — формулы correct? CSG: boolean ops (union, intersect, subtract) — edge cases (touching surfaces, coplanar faces)? **Р1-2:** Чисто. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест degenerate geometry: pyramid sides=2, tube innerRadius > outerRadius, sphere segments=0. |
| A46 | WebGL Layout | WebGL/Builder/Layout/*.agda | 393 | 3 | Grid: row/col count calculation. Spacing. Cell position. Radial: angle distribution — even? Custom? Stack: z-offset between layers. Overlap handling? **Р1-2:** **Исправлено**: Grid rows enforced via take. ringFacing π offset. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест spiral3D с 0 children — empty guard. Grid layout с non-square cell count и remainder handling. |
| A47 | WebGL Optimization | WebGL/Builder/Optimize/*.agda | 353 | 3 | Batching: which nodes can be batched? Same material requirement. Culling: frustum test — bounding sphere vs AABB. False positives/negatives. LOD: distance thresholds — smooth transition or pop? **Р1-2:** Чисто. FFI issues в backlog. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест LOD distance=999999.0 sentinel fallback. Culling FFI record field access — Scott-encoding destructure. |
| A48 | WebGL Controls: Input | WebGL/Controls/input.agda | 1719 | 3 | 3D Buttons: click detection via pick ID — correct wiring? Input: text input in 3D — keyboard events routed to focused element. Menus: 3D menu positioning — camera-facing? Sliders: drag in 3D space → value mapping. Toggles: on/off state + visual. Tabs: tab switching logic **Р1-2:** **Исправлено**: pushButton onLeave, applyActive Normal, color picker angles simplified. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест 3D pick ID uniqueness при dynamic scene changes. Dial drag accumulation across 360° boundary. |
| A49 | WebGL Controls: Display | WebGL/Controls/display.agda | 393 | 3 | Text: font atlas reference — matches reactive-gl.js font system? State: visual state indicators in 3D. Theme: color/material variables — resolved at compile time or runtime? **Р1-2:** Чисто. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест lookAtQuat billboard text facing camera. Font atlas glyph lookup miss — fallback glyph. |
| A50 | WebGL Charts + Audio | Charts+Audio/*.agda | 1910 | 3 | Bar3D: bar positioning in 3D space. Scatter3D: point placement. Network3D: force layout in 3D — convergence? Surface: mesh generation from data grid. Spectrum: FFT data → visual mapping. Waveform: sample rate handling, buffer size **Р1-2:** **Исправлено**: Bar3D maxVal scaling, Scatter3D identityTransform, line segment sqrt length. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест Network3D edge cylinder orientation — replace identityQuat с lookAt. Surface mesh normals для non-planar cells. |
| A51 | WebGL Gizmos + Import | Gizmos+Import/*.agda | 1449 | 3 | Transform gizmo: axis handles — correct hit testing? Rotation: quaternion from drag delta. Selection: multi-select, bounding box. Measure: distance/angle calculation. GLTF: parse correctness — all node types? Materials? Animations? NamedParts: part identification in complex models **Р1-2:** **Исправлено**: Transform gizmo addVec3/mulVec3/multiplyQuat composition. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест GLTF import с пустым mesh list. Selection bounding box recalculation при transform change. |

### Data + Forms + Misc

| # | Тема | Файлы | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|-------|-----|---|------------|------------|
| A52 | Animation | Anim/Spring+Tween+Easing.agda | 257 | 3 | Spring: stiffness/damping параметры — совпадают ли с JS runtime springLoop? Settle threshold? Tween: duration, easing — тип easing совпадает с JS interpretEasing? **Р1-2:** **Исправлено**: isSettledRange добавлен. Easing/Tween чисты. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Тест isSettledRange threshold match между Agda Spring и JS springLoop. Runtime integration: spring→tween handoff. |
| A53 | Data (Json, DateTime, Array, Buffer) | Json+DateTime+Array+Buffer.agda | 1596 | 3 | JSON: parse/emit — roundtrip fidelity? Unicode escaping? Large numbers? DateTime: leap year, DST transitions, timezone handling. Array: bounds checking — out of range? Negative index? Buffer: size calculation — overflow? **Р1-2:** Чисто. JSON/DateTime/Array/Buffer FFI корректны. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Runtime тест JSON roundtrip с emoji (4-byte UTF-8). DateTime: leap second edge case. Array negative index guard. |
| A54 | Forms | Forms.agda | 360 | 3 | Field validation: type-safe validators? Runtime validation vs compile-time? Submit: all fields valid check. Error display: per-field errors? Form-level errors? Reset: clear all fields + errors? **Р1-2:** Чисто. Validators regex корректны. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Runtime тест form submit with untouched required fields — isFieldValid touched=false semantics. Reset + resubmit cycle. |
| A55 | Primitive input | Primitive/Keyboard+Mouse+AnimationFrame.agda | 197 | 3 | KeyboardEvent record: поля совпадают с mkKeyboardEvent в events.js? MouseEvent record: поля совпадают с mkMouseEvent? AnimationFrame: handler type — receives timestamp (Float)? **Р1-2:** Чисто. Record fields match mkEvent. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Runtime тест AnimationFrame timestamp monotonicity. PointerEvent migration path — Mouse→Pointer unified handler. |
| A56 | Experimental | Experimental/Widget+IncLens.agda | 221 | 3 | Widget: API stable? Или breaking changes expected? IncLens: incremental lens — performance benefit demonstrated? Edge cases? **Р1-2:** **Исправлено**: Widget dead reference comment updated. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Решение: стабилизировать Widget API (добавить тесты) или пометить deprecated. IncLens benchmark vs обычный Lens. |
| A57 | Theory + Tests | Theory/*.agda + Test/*.agda | 1218 | 3 | Proofs: holes (`?`), `postulate`s (axioms without proof), `trustMe` (escape hatch)? Какие леммы доказаны, какие postulated? Test modules: что тестируют? Coverage? **Р1-2:** Чисто. Theory modules — чистые proof'ы. **Р3:** Верифицировано чисто. Все фиксы intact, FFI 1:1. | Автоматический подсчёт holes/postulates/trustMe по всему src/. Property-based тесты для Theory lemmas. |

---

## 4. Examples (8,722 LOC)

| # | Тема | Файлы | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|-------|-----|---|------------|------------|
| X1 | Styles (CSS generation) | Styles+IndexStyles.agda | 1940 | 3 | Генерируемый CSS: typos в property names/values? Missing selectors? Unused styles? Conflicting rules? Responsive breakpoints consistent? Color palette — accessible contrast ratios? **Р1-2:** Чисто. rootVars/themeVars дублирование — design. **Р3:** **Исправлено**: themeVars дедуплицированы (пустой список). Чисто. | Тест WCAG contrast ratio computed от rootVars. Responsive breakpoints coverage в generated CSS. |
| X2 | Core examples | Counter+Timer+Keyboard+Http+Task.agda | 561 | 3 | Counter: increment/decrement correct? Timer: interval cleanup on unmount? Keyboard: all key events handled? Http: error display? Loading state? Task: chain error propagation? **Р1-2:** Чисто. Task.agda fix не влияет на примеры. **Р3:** **Исправлено**: Task.agda disabledBind на кнопке. Чисто. | Тест Timer cleanup при unmount (subscription cancel). Http loading→error→retry cycle. |
| X3 | List examples | Todo+Router+Transitions.agda | 600 | 3 | Todo: add/remove/toggle — state consistent? Filter (all/active/completed)? Router: URL → view mapping complete? 404 handling? Transitions: enter/leave classes — timing? **Р1-2:** **Исправлено**: Router 404 path → "/404". **Р3:** **Исправлено**: Router active class via classBind. Table comment fixed. Чисто. | Тест Router browser back/forward navigation. Transitions timing при slow animations. |
| X4 | Async examples | Parallel+Worker+WebSocket.agda | 532 | 3 | Parallel: all tasks complete? Partial failure? Worker: message protocol — type mismatch? WebSocket: reconnect logic? Message ordering? Disconnect handling? **Р1-2:** **Исправлено**: WebSocket pendingMsg. **Р3:** **Исправлено**: Parallel error visible, race with raceTimeout 5000. Чисто. | Тест WebSocket reconnect after WsClosed. Parallel partial success display. |
| X5 | SVG examples | Svg*.agda | 1050 | 3 | Chart: data → SVG rendering correct? Empty data? LineDraw: drawing state machine? Undo? PanZoom: coordinate transforms — zoom center correct? Smil: animation timing? Chaining? **Р1-2:** **Исправлено**: PanZoom zoom factors swapped. SvgLineDraw dead code noted. **Р3:** **Исправлено**: SvgLineDraw dead code removed. Чисто. | Тест SvgChart wheel zoom sensitivity. SvgPanZoom zoom center при non-centered viewBox. |
| X6 | WebGL examples | WebGL*+ControlsDemo.agda | 1755 | 3 | GL scene setup: camera positioned correctly? Lights configured? Objects visible? Interaction: click → correct object? Drag → correct transform? Controls: all demo controls functional? **Р1-2:** Чисто. ScaleDown до 0 — minor design. **Р3:** **Исправлено**: WebGLTest ScaleDown min 1 via ⊔. Чисто. | Тест WebGLFullDemo multiple glClick handlers ordering. ControlsDemo toast auto-dismiss integration. |
| X7 | Agent examples | Agent+Session*.agda | 775 | 3 | Agent wiring: messages route correctly? Session: protocol followed? Dual session: symmetry works? Form: submit triggers session correctly? **Р1-2:** Чисто. **Р3:** Чисто. | Тест SessionDual put/get roundtrip. AgentWiring loop feedback convergence. |
| X8 | Advanced examples | OpticDynamic+Composition+Combinators+StressTest.agda | 1000 | 3 | Optic composition: get/set roundtrip? StressTest: performance metrics? Memory growth? Combinators: all combinator types exercised? **Р1-2:** Чисто. OpticDynamic stable-ID OK. **Р3:** Чисто. | Тест StressTest iterate BigInt allocation rate. Combinators foldE + batch interaction. |

---

## 5. Server Examples (18,343 LOC)

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| S1 | Main server | server/Main.agda | 1559 | 3 | Server config: port, host binding. Agent initialization order — dependencies respected? Graceful shutdown — all connections closed? **Р1-2:** Чисто. **Р3:** Чисто. | Тест server graceful shutdown с active WS connections. Agent init dependency ordering. |
| S2 | HttpAgent | server/HttpAgent.agda | 2102 | 3 | HTTP handler: request parsing — malformed input? State transitions — valid? Response format — consistent JSON? Error codes — correct HTTP status? **Р1-2:** Чисто. **Р3:** Чисто. | Тест HTTP handler с Unicode request body. State transition при concurrent steps. |
| S3 | MultiAgent + SharedAgent | MultiAgent+SharedAgentDemo.agda | 9279 | 3 | Multi-agent coordination: message ordering between agents? Shared state: concurrent read/write — consistency? Race conditions in agent interaction? Deadlock in agent communication? **Р1-2:** Чисто. **Р3:** Чисто. | Тест SharedAgent deadlock при circular dependency. Multi-agent cascade latency. |
| S4 | InspectorDemo | server/InspectorDemo.agda | 5403 | 3 | Inspector: model serialization complete? Peek response format — matches JS parser? Over command — type safety at JSON boundary? Performance: large model serialization — blocking? **Р1-2:** Чисто. **Р3:** Чисто. | Тест Inspector peek на модели с 10000 fields — performance. Over command injection safety. |

---

## 6. Build Pipeline (530 LOC)

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| B1 | CSS generation | scripts/generate-css.js | 129 | 3 | Input: compiled Agda module with CSS declarations. Output: `.css` file. Selector escaping — special chars in class names? Property ordering — specificity? @import/@font-face — handled? Comments — stripped? Minification? **Р1-2:** **Исправлено**: --hash freshness skip. **Р3:** **Исправлено**: source comment format без #. Чисто. | Тест CSS generation с @font-face. Minification pipeline. Output byte count accuracy. |
| B2 | Animation data gen | scripts/generate-anim-data.js | 79 | 3 | JSON output: valid JSON? Numeric precision? Empty animation list? Large datasets — memory? **Р1-2:** **Исправлено**: undefined exit(1). **Р3:** Чисто. | Тест с пустым animation module. Numeric precision при Float→String→JSON roundtrip. |
| B3 | Build verification | scripts/verify-build.sh + typecheck-all.sh | 185 | 3 | All modules listed? New modules auto-discovered? Exit codes — correct? Error messages — actionable? Parallel execution — race conditions in file system? **Р1-2:** **Исправлено**: missing modules added. **Р3:** **Исправлено**: auto-discovery модулей из package.json и find. Чисто. | Тест auto-discovery при добавлении нового .agda файла. Parallel typecheck race conditions. |
| B4 | ESM conversion | scripts/cjs-to-esm.cjs + fix-esm.cjs | 102 | 3 | `require()` → `import` — all patterns handled? `module.exports` → `export` — named vs default? Dynamic require? Circular dependencies? Source maps — preserved? **Р1-2:** Чисто (design gaps в backlog). **Р3:** **Исправлено**: variable prefix regex broadened, exports exact match, source maps added. Чисто. | Тест ESM conversion с circular dependency (A imports B, B imports A). Source map validation с debugger. |

---

## 7. Tests (3,172 LOC)

| # | Тема | Файл | LOC | П | Что уже искали | Что смотреть дальше |
|---|------|------|-----|---|------------|------------|
| T1 | Runtime tests | test/runtime.test.js | 760 | 3 | Покрытие: какие функции НЕ тестируются? Assertions: правильные ли expected values? Flaky: timing-dependent tests? Isolation: тесты влияют друг на друга через shared state? **Р1-2:** Чисто. 41 тестов (1 obsolete удалён). **Р3:** **Исправлено**: legacy тесты удалены (39 total). Чисто. | Добавить тесты для SMIL conditional start, localStorage try/catch recovery, BOOL_ATTRS inert/popover. |
| T2 | Fuzz tests | test/fuzz-runtime.mjs | 783 | 3 | Покрытие: все ли exported функции фаззятся? Quality inputs: достаточно ли edge cases? Missing types: WeakRef, Proxy, generators, async iterators? **Р1-2:** Чисто. 86 fuzz тестов. **Р3:** Чисто. 137 fuzz тестов. | Добавить fuzz для new backlog functions: clampProgressValue, guardStep, guardMinMax, sortBy comparator. |
| T3 | GL math tests | test/gl-math.test.js | 164 | 3 | Edge cases: NaN input, Infinity, zero vectors, parallel vectors для lookAt, singular matrices, extreme aspect ratios. Precision: tolerance в float comparison — 1e-6 достаточно? **Р1-2:** Чисто. 20 GL math тестов. **Р3:** Чисто. 26 GL math тестов. | Добавить тест lookAtQuat parallel fallback, emptyGeometry null return, isTransparent instanced. |
| T4 | Data tests | test/json+array+datetime+forms.test.js | 657 | 3 | JSON roundtrip: special chars, unicode, nested objects, arrays, null, numbers. DateTime: leap year, DST, timezone edge cases. Array: empty, single element, out of bounds. Forms: empty submit, invalid field types **Р1-2:** Чисто. **Р3:** Чисто. | Тест DateTime с timezone offset edge case (UTC±12). Array sortBy stability. |
| T5 | CSS + clone + smoke | test/css-pipeline+clone+smoke.test.js | 703 | 3 | CSS pipeline: all module CSS generates valid CSS? Clone: both Scott-encoding formats? Smoke: all 381 modules load — но тестирует ли что-то кроме загрузки? **Р1-2:** Чисто. 382 smoke тестов (было 381). **Р3:** Чисто. 382 smoke тестов. | Тест smoke coverage — все ли new backlog modules (sortableGrid, toastAutoDismiss etc.) загружаются. |

---

## Сводка

| Категория | Тем | LOC | Мин П | Приоритет |
|-----------|-----|-----|-------|-----------|
| JS Runtime | 40 | ~10K | 3 (R5,R11,E1,V2: П=4) | 🔴 Высший |
| Haskell Server | 6 | ~1.5K | 3 | 🔴 Высший |
| Agda FFI | 3 | ~530 | 3 | 🔴 Высший |
| Agda Core | 54 | ~37K | 3 | 🟡 Средний |
| Examples | 8 | ~8.7K | 3 | 🟢 Низкий |
| Server examples | 4 | ~18K | 3 | 🟡 Средний |
| Scripts | 4 | ~570 | 3 | 🟢 Низкий |
| Tests | 5 | ~3.5K | 3 | 🟡 Средний |
| **Итого** | **124** | **~63K** | **все П≥3** |
