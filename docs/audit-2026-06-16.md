# Аудит CRM-ядра (Ф0–Ф5) — 2026-06-16

Мульти-агентный адверсариальный аудит: 4 finder-агента × дименшен → независимая верификация каждой находки скептиком (refute-by-default) → синтез. 25 агентов, ~800k токенов. 15 подтверждено из 20 (5 опровергнуто). Дубликаты: M5=M6=L4 (один баг walGo).

{
  "summary": "Adversarial multi-agent audit of the CRM stack (phases 0-5) across correctness, security/money, architecture, durability",
  "agentCount": 25,
  "logs": [],
  "result": {
    "confirmedCount": 15,
    "totalReported": 20,
    "summary": "# Отчёт аудита: CRM-ядро (WAL + in-memory, single-writer)

## CRITICAL
Подтверждённых критических находок нет.

## HIGH

**H1. Headless-API слушает на всех интерфейсах без аутентификации** — `Warp.setHost "*"` (`hs/Agdelte/Http.hs:44`), достижимо через `server/CrmServer.agda:20` (`listen 8137`) и `services-core/Crm/Api.agda:181`. Удалённый неаутентифицированный доступ к деньгам и PII (`/parties`, `/accounts`, `/charge`).
Замечание: отсутствие authz само по себе — задокументированный отложенный пункт (`docs/plan-crm-core.md:175`, SPEC §13); инкрементальный дефект — именно дефолтный bind на `0.0.0.0` вместо `127.0.0.1`.
Фикс: bind по умолчанию на `127.0.0.1` (host-параметр в `listen`/`serve`/`serveWithWs` вместо хардкода `"*"`) и/или shared-secret/bearer-проверка в `Crm.Api.route` до любого хендлера; как минимум закрыть `POST /accounts`, `POST /charge`, `GET /parties` токеном.

## MEDIUM

**M1. Открывающий баланс счёта берётся из тела запроса без границ и аудита** — `services-core/Crm/Api.agda:144` (`postAccount`) → `Crm/Commands.agda:72` (`createAccount`): `acBalance` = присланное число, без cap, без ledger-проводки.
Фикс: новый счёт — баланс `0`; открывающий баланс только через аутентифицированную аудируемую credit-проводку (которая сейчас даже не зароутена).

**M2. `jsonGetField`/`jsonGetNat` — подстроночные сканеры, а не JSON-парсер** — `src/Agdelte/FFI/Json.agda:27,42`. Реально: усечение значений на первой кавычке (`{"name":"a\"b"}` → `a\`, порча данных в WAL). Замечание: конкретный PoC «field smuggling» из находки некорректен — `\"amt\"` не матчится needle `"amt"` из-за обратного слэша; smuggling требует уже невалидного JSON, который настоящий парсер отверг бы. Базовая слабость (позиционный first-match, без учёта escape) реальна.
Фикс: парсить тело настоящим JSON-парсером (aeson) на границе FFI, декодировать escape, ограничить скан top-level ключами; отвергать тела, не парсящиеся как JSON-объект.

**M3. Наружу утекает внутренний bigint `id`, нарушая инвариант SPEC §13** («bigint id никогда не покидает сервер», `docs/SPEC.md:564`) — `services-core/Crm/Api.agda:84` (POST-конверт отдаёт `{"id":N}` и вообще без uuid), `:105`/`:110-111` (encoders), `:158` (`/charge` адресует счёт по внутреннему id).
Фикс: убрать `pId/eId/acId` из read-encoders и POST-конверта, отдавать `uuid`; адресовать `/charge` по uuid с резолвом uuid→id на сервере (планируемый byUuid-индекс).

**M4. Конверт ошибок плоский, маршруты не `/api/v1/...`, нет пагинации** — `services-core/Crm/Api.agda:70` (`{"error":"..."}` вместо `{"error":{"code,message}}`), `:181-193` (маршруты `/parties` и т.п.), `:124,168` (`IM.toList` отдаёт всю таблицу без `limit`/`cursor`/`next_cursor`). Контракт-конформанс §13; не DoS (single-tenant, объём ограничен данными одного оператора).
Фикс: структурный конверт ошибок с кодами; роутинг под `/api/v1/<plural-kebab>/{uuid}`; курсорная пагинация через имеющийся `IndexedMap.entriesFrom`, отдавать `next_cursor`.

**M5. WAL-recovery молча обрезает лог при повреждении length-заголовка committed-записи** — `src/Agdelte/FFI/FileSystem.agda:113-117` (`walGo`). Раздутая `n` → `BS.length rest < n` → `Just (reverse acc)`: коммитятся только записи ДО повреждения, всё после — молча теряется, `walOpen` рапортует успех. Нарушает заявленную гарантию «mid-stream corruption → Nothing → die» (`WAL.agda:18-23`). Реальная экспозиция — bitrot уже committed-данных (НЕ crash: single-writer + `modifyMVarMasked` + fsync делают torn-tail корректным). Корневая причина — отсутствие per-record CRC (задокументированный отложенный пункт), но дефект сильнее: фальсифицирует более сильную заявленную гарантию.
Замечание: M5 и M6 (и L4 ниже) — один и тот же баг `walGo`, поданный с разных сторон; это одна находка, а не три.
Фикс: per-record CRC, либо length-sanity-bound (короткое тело легитимно только как самые последние байты файла), чтобы раздутая длина не выдавала себя за torn tail.

**M6. = M5.** Та же misclassification «corrupt length-header → torn tail → silent truncation» (`src/Agdelte/FFI/FileSystem.agda:114-116`), описанная через сценарий `5:`→`95:`. Отдельной находкой не считается — дубликат M5.

## LOW

**L1. Нет инварианта уникальности внешнего uuid** — `services-core/Crm/Identity.agda:44` (и `:63,78,111`); нет uuid-индекса (`Crm/Store.agda:27` откладывает byUuid «на API-слой»), нет `guardT` в create-командах. Замечание: триггеры спекулятивны — путь client-supplied uuid не существует (везде `genUuid`), источник — kernel CSPRNG (коллизия v4 пренебрежимо мала). Латентный пробел перед тем, как uuid станет внешним адресом.
Фикс: uuid-вторичный индекс + `guardT Conflict` в create-командах (ляжет вместе с отложенным byUuid), либо явно зафиксировать неуникальность uuid и убрать его из контракта внешней адресации.

**L2. `genUuid` без обработки ошибок** — `services-core/Crm/Api.agda:55` (`withFile`/`hGetLine`, без `try`), вызывается из `:133`/`:150`. Любой сбой чтения `/proc/.../uuid` → исключение → opaque 500 на каждый `POST /parties` и `/accounts`. Замечание: на нормальном Linux-хосте файл всегда есть и читаем; сценарий transient-сбоя в основном гипотетичен — это эргономика ошибок, не корректность. (`postCharge` не зависит, продолжает работать.)
Фикс: обернуть в `tryCatch` → чистый `503 {"error":"id source unavailable"}`; опционально — userspace UUIDv4 из random-байтов.

**L3. `GET /parties` вернёт soft-deleted строки** — `services-core/Crm/Api.agda:167`,`:124` (`listJson`/`IM.toList` без live-фильтра) против инварианта P4 (`src/Agdelte/Storage/IndexedMap.agda:109-110`). Сейчас не эксплуатируется (ни одна команда не ставит `*DeletedAt := just`, `Del*` — hard delete), forward-looking.
Фикс: фильтровать `live (pDeletedAt p)` в `getParties`/`listJson` (и в будущих list-эндпоинтах, включая `getAccounts`/`aDeletedAt`).

**L4. = M5/M6 (дубликат).** `src/Agdelte/FFI/FileSystem.agda:114-116`, сценарий `5:`→`95:` — та же находка `walGo`, что M5. Перечислена только для трассировки; самостоятельной находкой не является.

**L5. Soft-delete смоделирован и проверяется везде, но ни одна команда его не выставляет; DELETE только hard** — `services-core/Crm/Commands.agda:132` (`hardDeleteEngagement` → `Store.agda:114` `IM.delete`). Противоречит SPEC §5.7 (анонимизация вместо удаления, `SPEC.md:387,531`). Замечание: это in-progress Phase-4, live-guards — корректный защитный код, не вредный dead code. Важная поправка: доки переоценивают лёгкость фикса — `plan-crm-core.md:129` утверждает «Soft-delete = обычный Set* … уже есть» (ложь, такого op нет), а экстракторы `Store.agda:30-37` возвращают FK безусловно (не смотрят `deletedAt`), т.е. soft-delete потребует правки экстракторов + Set*/SoftDel-op + live-page top-up, а не one-liner.
Фикс: решить soft vs hard per-entity; для §5.7-сущностей добавить soft-delete-op, сделать live-проверки и `entriesFrom` live-page осмысленными.

**L6. `app/migrations/0001_identity.sql` содержит полную Postgres/RLS-схему в «текущем» app-слое** — `app/migrations/0001_identity.sql:1`, при том что движок — WAL+in-memory. Нет маркера «отложенный путь»; `app/README.md:7-8` подаёт миграции как актуальную работу app. Расхождение уже реальное: `Crm/Identity.agda:7` («Tenancy omitted for the single-operator MVP») против `tenant_id`/RLS в SQL. Не runtime-баг (SQL не исполняется), extractability-hazard.
Фикс: перенести SQL+миграционную рамку в явно-отложенное место (напр. `docs/postgres-spike/`), либо добавить header/README-пометку «deferred scale-out path, НЕ используется WAL-движком».

**L7. Вторичные индексы `IndexedMap` не освобождают пустые bucket-ключи** — `src/Agdelte/Storage/IndexedMap.agda:50-52` (`removeKey` всегда `NM.insert key [] im`, не `NM.delete`). Монотонный рост по истории touched-ключей; обостряется планируемым высококардинальным `byUuidHash`-индексом; replay воспроизводит те же пустые buckets. Малый абсолютный след, на корректность `byIndex` не влияет.
Фикс: в `removeKey` делать `NM.delete` ключа, когда итоговый список `[]` (поведение инвариантно — `byIndex` уже отдаёт `[]` через `fromMaybe []`).

**L8. Переполнение `Int` в fold length-префикса WAL** — `src/Agdelte/FFI/FileSystem.agda:113-116`. Заголовок с ≥19-значным digit-run переполняет `Int` → отрицательная `n` → `BS.length rest < n` ложно → спурьёзная пустая запись + рекурсия по неизменённому `rest`. Поправка к находке: фабрикуемая запись ПУСТАЯ (декодит в ноль ops, `WAL.agda:91-92`), так что в восстановленное состояние мусорный op НЕ инъектируется — формулировка «fabricates bogus records»/«corrupting recovered state» неверна; реальный вред — «не падает на повреждённом логе». Внутри задокументированного «no CRC» отложенного пункта.
Фикс: fold в `Integer`, bound-check до сужения; cap на число цифр (>~19 цифр = заведомо порча → `Nothing`).

**L9. `Wire.readField` не отвергает over-declared inner-длину** — `src/Agdelte/Storage/WAL.agda:88-98` / `Wire.agda:61` (`splitAt n` молча клампит при `n > length rest`). Поправка: headline «silently truncates a record's ops» НЕ достижим ни одним зарегистрированным кодеком — Del* (`decℕ`) и Set* (`Wire.runR`, требует полного потребления) и AppStore (точная арность) отвергают проглоченные framing-байты → `nothing → die` (fail-closed); потеря trailing-ops = ноль. Реальна только латентная слабость фрейминга (целостность делегирована strictness каждого кодека), defense-in-depth.
Фикс: после `splitAt n rest` требовать `length (proj₁ sp) ≡ n`, иначе `nothing` — самоэнфорсит fail-closed вместо зависимости от кодеков.

## Вердикт

Ядро архитектурно здравое и в части durability ведёт себя fail-closed по дизайну (single-writer + fsync + masked-append корректно обрабатывают torn tail). Критических багов нет; единственный high — это hardening-промах (дефолтный bind на `0.0.0.0`), а почти все medium/low — либо контракт-конформанс к ещё не реализованному §13, либо заострённые формулировки уже задокументированных отложенных пунктов (per-record CRC, byUuid, authz), а не новые классы риска. Главное к запуску: bind на `127.0.0.1` + shared-secret, не принимать открывающий баланс из сети, и закрыть WAL-дыру silent-truncation (M5) реальным per-record CRC; остальное — плановый долг до прихода настоящего authz/byUuid.

Примечание для трассировки: из 15 поданных пунктов независимых находок 13 — пункты 5/6/14 (M5/M6/L4) описывают один и тот же баг `walGo`; критических нет.",
    "confirmed": [
      {
        "title": "Headless CRM API binds to ALL interfaces with no authentication — remote money + PII abuse",
        "file": "hs/Agdelte/Http.hs:44 (Warp.setHost "*"), reached via server/CrmServer.agda:19 (listen 8137) and services-core/Crm/Api.agda:181 route",
        "severity": "critical",
        "scenario": "Run CrmServer (binds 0.0.0.0:8137 via Warp.setHost "*"). From any host that can route to the box: `curl -s host:8137/parties` returns every party's name+uuid (PII); `curl -d '{"balance":1000000}' host:8137/accounts` mints an account with an arbitrary opening balance; `curl -d '{"acc":1,"amt":999999}' host:8137/charge` drains account #1. No bearer/token/origin check exists anywhere in Crm.Api.route or CrmServer (grep for auth/token/Guard finds nothing).",
        "rationale": "The documented deferral is 'authz not designed (single-operator MVP)', which is acceptable ONLY if the socket is not remotely reachable. But the listener binds to '*' (all interfaces), not 127.0.0.1, so the money path (POST /charge, POST /accounts) and the PII read path (GET /parties) are exposed to the whole network with zero authentication. This makes the deferral materially worse/exploitable than documented: it is a concrete unauthenticated remote write-to-balances and read-all-PII endpoint, not a localhost single-operator tool.",
        "fix": "Bind the CRM listener to 127.0.0.1 by default (add a host-bound variant of listen/serve instead of Warp.setHost "*"), and/or require a shared-secret/bearer check in Crm.Api.route before any handler runs. At minimum gate POST /accounts, POST /charge and GET /parties behind a token until real authz lands.",
        "dimension": "security",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "high",
          "note": "Verified the full chain in actual code. Http.hs:44 `Warp.setHost "*"` is real — in Warp "*" = HostAny = bind 0.0.0.0 (all interfaces). Reached via server/CrmServer.agda:20 `listen 8137`, and src/Agdelte/FFI/Server.agda:226 binds `listen` to `Http.serve` → `serveWithWs` (Http.hs:41) → setHost "*". Crm.Api.route (services-core/Crm/Api.agda:181-193) dispatches directly to handlers with NO auth/token/origin/guard check anywhere; grep for auth/token/bearer/secret/127.0.0.1/origin in Crm/Api.agda + server/ returns nothing. The money/PII handlers are exactly as described: postAccount (lines 144-152) mints an account with an attacker-supplied opening `balance` (jsonGetNat "balance"); postCharge (155-164) debits an arbitrary account/amount; getParties (167-168, encoder 103-106) returns every party's id+uuid+name (PII). Concrete exploit on a routable box: `curl -d '{"balance":1000000}' host:8137/accounts` then `curl -d '{"acc":1,"amt":999999}' host:8137/charge`, and `curl host:8137/parties` to dump PII — all unauthenticated.

Severity downgraded critical→high. The authz gap itself is a DOCUMENTED deferral (docs/plan-crm-core.md:175 'механизм ACL не спроектирован', SPEC §13 lists authz as not-yet-built, single-operator MVP), so the missing auth alone is intended-for-now and not a new bug. The genuinely incremental, materially-worse-than-documented defect is the bind-to-all-interfaces DEFAULT ("*" instead of 127.0.0.1): the 'single-operator MVP' framing implies a local tool, and no doc states a reverse-proxy/firewall assumption, yet the socket is exposed to the whole network. That is a real, easy-to-fix hardening miss. Not critical because exploitation is contingent on the operator running it on a routable host (no Dockerfile/systemd/exposure evidence found; pre-launch MVP state), and it does not by itself mint money — it widens an already-known deferral's blast radius. Proposed fix is sound: default-bind 127.0.0.1 (add a host param to serve/listen instead of hardcoded "*"), and/or add a shared-secret gate in route before any handler. Note: the same "*" bind also affects the agent servers (runAgentServerN, Server.agda:251/260) via Just "*", so the localhost-default fix should cover serveWithWs centrally."
        }
      },
      {
        "title": "WAL recovery silently truncates the log when an inner-corrupted record carries an over-large byte-length header (mid-stream corruption masquerades as a torn tail)",
        "file": "src/Agdelte/FFI/FileSystem.agda:113-117",
        "severity": "high",
        "scenario": "A WAL file gets a single bit-flip inside a record's length digits (e.g. record header '37:' becomes '37000000000000:' or a power-loss flush leaves a valid '<digits>:' prefix whose declared length exceeds the bytes actually written for THIS record, while well-formed committed records follow it). walGo parses the all-digit header, computes a huge n, hits `BS.length rest < n`, and returns `Just (reverse acc)` — i.e. it reports success with only the records BEFORE the bad header and SILENTLY DISCARDS every committed transaction after it. The design contract (WAL.agda:18-23, this file:86-93) promises only a *torn tail* is dropped and that genuine mid-stream corruption yields Nothing → die. Here a corrupted length field (as opposed to a corrupted boundary char) is indistinguishable from a torn tail, so the engine comes up clean while having dropped durably-committed data.",
        "rationale": "There is no per-record CRC (documented deferral) AND no upper bound relating the declared length to the remaining file size, so the only signal that 'this isn't really the tail' (bytes existing after where the body should end) is never checked. The failure is silent acknowledged-data loss, which is strictly worse than the documented torn-tail behavior and defeats the 'refuse to start, never silently skip' guarantee the module advertises.",
        "fix": "When `BS.length rest < n` but `bs` is not the only/last frame, distinguish 'truncated tail' (this is the final frame and the whole remainder is shorter than n) from 'inconsistent length' — e.g. require that an incomplete body occurs only at EOF, and if any non-empty bytes were expected to follow a fully-consumed record but the lengths don't line up, return Nothing (die). Minimal hardening: add a per-record length sanity cap and/or a trailing CRC so an inflated length cannot impersonate a torn tail.",
        "dimension": "correctness",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "medium",
          "note": "CONFIRMED real at src/Agdelte/FFI/FileSystem.agda:113-117 (walGo). I ported the exact walGo logic to GHC 9.10 and brute-forced single-bit flips. A single-bit corruption of a LENGTH DIGIT that inflates the declared length of a COMMITTED, mid-stream record makes `BS.length rest < n` fire and returns `Just (reverse acc)` — silently dropping every committed record that follows, with NO Nothing/die. Empirical: clean WAL `1:A1:B1:C` -> flipping the 2nd record's length digit `1`->`5` (`1:A5:B1:C`) yields Just ["A"] (B,C silently lost); `:` (0x3a) also single-bit-flips to `8`/`2` (digits), merging length fields; an 18-digit inflated length on the FIRST record yields Just [] (whole durable log silently discarded, engine starts blank, no error). This violates the module's explicit guarantee in WAL.agda:18-23 and FileSystem.agda:86-93 that mid-stream corruption -> readWalRecords nothing -> walOpen die ("refuse to start, never silently skip"): an inflated length is mis-classified as a torn tail.

Why I corrected the finding's framing and downgraded high->medium: (1) The finding's crash/power-loss sub-scenario is NOT a real exposure — all appends go through one MVar via modifyMVarMasked (Server.agda:144) with appendWalRecord (fsync-before-return) INSIDE the masked callback before publishing state (WAL.agda:153-164), so the writer is strictly serial+synchronous. A crash leaves at most one in-flight append, whose partial bytes are always at EOF with no committed records after them; that torn tail is correctly dropped and was never acknowledged as committed. So the documented torn-tail behavior is correct. (2) The REAL exposure is on-disk corruption of already-committed records (bitrot/storage fault), which is exactly where records follow the bad header — there the silent-truncation property holds. (3) Root cause = no per-record CRC, which IS a documented deferral; but this is materially worse than "undetected corruption exists" because it falsifies the module's STRONGER stated guarantee (non-tail corruption -> die). (4) Single-operator MVP, no untrusted WAL writer, low trigger probability (not crash-induced) -> medium, not high. Proposed fix direction is sound: per-record CRC, or a length-sanity bound relating n to remaining file size so an inflated length cannot impersonate a torn tail."
        }
      },
      {
        "title": "POST /accounts mints an arbitrary balance straight from the request body",
        "file": "services-core/Crm/Api.agda:144 (postAccount) → :72 createAccount (Commands.agda:72)",
        "severity": "high",
        "scenario": "`curl -d '{"balance":4294967296}' :8137/accounts` creates an account whose acBalance is exactly the attacker-supplied number, with no cap, no audit, no provenance. There is no double-entry/ledger op — balance is set, not derived from credits. Combined with the unauthenticated '*' bind above, this is unauthenticated money creation.",
        "rationale": "The 'correct-by-construction, balance never negative' property protects only the charge path; the opening balance is fully attacker-controlled and unbounded. Money should normally enter via an auditable credit, not an opening-balance field on an open endpoint. There is also no upper bound, so a balance can be set to absurd values that later interact with reporting/charge arithmetic.",
        "fix": "Do not accept opening balance from untrusted input on a network endpoint: default new accounts to 0 and require an explicit, authenticated, audited credit op (which itself is currently not even routed). If an opening balance is legitimately needed, gate it behind authz and record it as a credit ledger entry.",
        "dimension": "security",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "medium",
          "note": "Confirmed in actual code. services-core/Crm/Api.agda:144-152 (postAccount) reads "balance" via jsonGetNat (parses an arbitrary Integer→ℕ, no bound) and passes it straight to createAccount. Crm/Commands.agda:72-76 sets acBalance = balance via mkAccount with zero validation, no cap, no ledger/credit op. acBalance is plain ℕ (Identity.agda:111), so no refinement bounds it. A `credit` command exists (Commands.agda:163) but is NOT routed — the router (Api.agda:181-193) exposes only GET/POST /parties, /accounts, /charge. So `curl -d '{"balance":4294967296}' /accounts` does create an account with exactly that balance. The "unauthenticated/any-interface" part is also true (Http.hs:44 / AgentServer.hs:321 bind setHost "*", no auth). The real, non-duplicative defect: opening balance is set (not derived from an auditable credit) and unbounded. Severity downgraded high→medium: the "unauthenticated" angle largely restates the DOCUMENTED authz deferral (single-operator MVP, authz/ACL not designed), which I must not re-report as new. What remains genuinely worth flagging beyond that deferral is the money-provenance/no-cap design: balance enters via an opening-balance field rather than an audited credit ledger entry, and there is no upper bound (absurd values can later feed reporting/charge arithmetic). Proposed fix (default to 0, route an authed/audited credit) is sound. Not 'high' given the single-operator threat model; not 'not-a-bug' because the set-vs-derive + unbounded design is a legitimate correctness concern independent of authz."
        }
      },
      {
        "title": "jsonGetField / jsonGetNat are substring scanners that do not parse JSON — wrong field/value picked, names truncated at first quote",
        "file": "src/Agdelte/FFI/Json.agda:27 (jsonGetFieldImpl, T.breakOn + T.takeWhile (/='"')) and :42 (jsonGetNatImpl)",
        "severity": "medium",
        "scenario": "(a) Truncation/shift: POST /parties with `{"name":"a\"b","type":"O"}` — jsonGetFieldImpl does takeWhile (/= '"'), so name becomes just `a` (the JSON escape \" is not understood), silently corrupting data written to the WAL. (b) Field smuggling: because matching is positional substring (breakOn on `"field"`), a value that itself contains the literal token `"amt":` placed before the real key changes which occurrence wins, e.g. POST /charge `{"memo":"see \"amt\":0  refund","acc":1,"amt":500}` — the first `"amt"` breakOn hits is inside memo, and its following char is `:` then `0`, so jsonGetNat returns 0 instead of 500. The amount actually charged is decided by string content, not JSON structure.",
        "rationale": "Trusting a substring scanner as a JSON parser means the value charged/created can diverge from the apparent JSON, and legitimate strings containing quotes are silently truncated before being persisted to the WAL. On a single-operator box this is mostly a correctness/data-integrity bug, but it is request-reachable and affects the money path (amt/acc) and what gets durably logged.",
        "fix": "Parse the body with a real JSON parser (aeson) at the FFI boundary and extract typed fields, or at minimum scope the scan to top-level object keys and decode JSON string escapes. Reject bodies that don't parse as a JSON object.",
        "dimension": "security",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "medium",
          "note": "Confirmed in real code. src/Agdelte/FFI/Json.agda:26-39 jsonGetFieldImpl = T.breakOn needle then T.takeWhile (/='"'); :41-53 jsonGetNatImpl = T.breakOn needle, T.dropWhile (==':'||==' '), T.takeWhile isDigit. Both are substring scanners, not JSON parsers. Reachable from services-core/Crm/Api.agda: postParty:130 (name), postAccount:147 (balance), postCharge:158 (acc/amt → money path, written to WAL). Body is raw reqBody (hs/Agdelte/Http.hs:105, TE.decodeUtf8With lenientDecode) with NO upstream JSON parse/validation anywhere — confirmed by grep (only Wire/codec decoders exist, no aeson/FromJSON on request bodies).

SCENARIO (a) TRUNCATION — REAL. {"name":"a\"b",...}: raw value text after opening quote is a\"b", takeWhile (/='"') stops at the escaped quote, yielding a\ (backslash retained), so the name is silently truncated/corrupted and that corrupted value is persisted to the WAL via createParty. Solidly real; this is the worst practical impact (silent data corruption, no escape decoding).

SCENARIO (b) SMUGGLING — REPORT'S EXACT PoC IS WRONG. The cited body {"memo":"see \"amt\":0  refund",...} contains \"amt\" — the needle is the 5 chars "amt" (quote-a-m-t-quote), but the text has a backslash between t and the closing quote (amt\"), so breakOn does NOT match that occurrence and correctly finds the real "amt":500. The report's claim that amt returns 0 for that input is incorrect. Smuggling only works with a deliberately MALFORMED body where the literal bytes "amt": appear before the real key (e.g. {"x":"a"amt":0z","amt":500}), which a real JSON parser would reject. So the underlying weakness (positional first-match, no structural/escape awareness) is genuine, but the specific exploit is overstated.

Severity medium is fair: request-reachable, touches money/WAL paths, but single-operator MVP per design intent; truncation is the real correctness hazard, smuggling requires malformed input. Proposed fix (real JSON parser at FFI boundary, decode string escapes, scope to top-level keys) is appropriate."
        }
      },
      {
        "title": "Headless API leaks internal bigint `id` to clients, violating SPEC §13's stated invariant ('bigint id никогда не покидает сервер')",
        "file": "services-core/Crm/Api.agda:84",
        "severity": "high",
        "scenario": "POST /parties {"name":"X"} returns {"data":{"id":7}}; GET /parties returns partyJson with "id":7 (Api.agda:105). POST /charge requires {"acc":7,"amt":...} reading the INTERNAL id from the body (Api.agda:158). So the only way a client can address an account to charge it is by its internal id — the exact value SPEC §13 says must never leave the server.",
        "rationale": "SPEC §13 is explicit: 'Наружу — только uuid. bigint id никогда не покидает сервер.' The whole #N2 design (deterministic internal id + uuid minted at the IO boundary) exists precisely so the internal id stays internal. Emitting and accepting it (a) breaks the contract, (b) couples external clients to an internal monotonic counter, and (c) is an information leak (table sizes / creation order). Crucially it is self-inflicting: when byUuid lookup lands (the documented next step), every route and the JSON encoders must switch from id to uuid, and existing clients built against "id" break. This is the kind of open-end that forces a client+API rewrite rather than an addition.",
        "fix": "Strip pId/eId/acId from all read encoders (partyJson/accountJson) and from the POST result envelope — return "uuid" instead of "id". Make POST /charge (and future routes) address accounts by uuid, resolving uuid→id server-side (the byUuid index the docs already plan). Keep internal ids purely internal as §13 mandates.",
        "dimension": "architecture",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "medium",
          "note": "Confirmed against actual code. SPEC §13 invariant is verbatim at docs/SPEC.md:564 ("Наружу — только uuid. bigint id никогда не покидает сервер"), and the SPEC header (docs/SPEC.md:9) explicitly keeps the API contract §13 IN FORCE (only the DB-specific parts of §13 are the parked Postgres path), so this is a binding, current contract. The code violates it at every cited point in services-core/Crm/Api.agda: line 84 the POST envelope returns {"id":N} (internal bigint) and notably NO uuid at all — the freshly-minted uuid from createParty/createAccount is discarded since commit's Txn returns only ℕ; line 105 partyJson emits "id":pId; lines 110-111 accountJson emits "id":acId; line 158 postCharge addresses the account solely by internal id (jsonGetNat "acc"). So clients can only address an account by the exact value §13 forbids leaving the server. (The Wire.agda:46/127 id emissions are the binary WAL persistence codec, internal — not a §13 concern.)

Why MEDIUM, not HIGH: the claimed "information leak" (creation-order / table-size disclosure) carries no real security weight under the current threat model — single-operator MVP, no multitenancy, authz/ACL explicitly undesigned (known deferral), one trusted caller. There is no untrusted "client" to leak to yet. It partially overlaps the documented byUuid deferral (plan-crm-core.md:156-159), though it is materially worse than that note admits: the plan claims "uuid возвращается" while in fact POST returns id-only and charge is id-addressable, i.e. the author appears unaware the bigint id also leaks/round-trips. The "forces a full client+API rewrite" rationale is real design debt but forward-looking/speculative, not a present correctness or safety bug. Net: a genuine deviation from a binding contract that will require rework when byUuid lands, but no current exploit — medium. Proposed fix (return uuid in envelope+encoders, address charge by uuid via the planned byUuid index) is sound and aligns with the already-planned hashString/byUuidHash work at plan-crm-core.md:158."
        }
      },
      {
        "title": "Error envelope is a flat string, not SPEC §13's structured {code,message}; routes are not /api/v1/<entity-plural-kebab>; no pagination",
        "file": "services-core/Crm/Api.agda:70",
        "severity": "medium",
        "scenario": "Any error returns {"error":"not found"} (errJson, Api.agda:70) instead of the §13 shape {"error":{"code":"not_found","message":"..."}}. Routes are /parties, /accounts, /charge (route, Api.agda:181-193) instead of /api/v1/parties etc. GET /parties returns the ENTIRE table via IM.toList (Api.agda:124,168) with no ?limit=&cursor= and no next_cursor.",
        "rationale": "SPEC §13 fixes the public contract: structured error codes (not_found/validation/conflict/...), /api/v1/<entity> plural-kebab routing, {uuid} sub-resources, and ?limit=&cursor= pagination carrying next_cursor. The current API is an internal-shaped MVP that diverges from the contract on all three axes. The unbounded GET is also a latent DoS (16 MB request cap exists, but response size is unbounded). Each is an additive fix individually, but together they mean any client written against today's API is incompatible with the §13 contract the project commits to — a coordinated breaking change later.",
        "fix": "Introduce the {code,message} error envelope and the structured codes; move routing under /api/v1 with plural-kebab entity names and {uuid} sub-resources; implement ?limit=&cursor= using the already-present IndexedMap.entriesFrom (cursor=uuid→id→entriesFrom) and emit next_cursor. Doing this before more verticals are built avoids a wider rewrite.",
        "dimension": "architecture",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "medium",
          "note": "Confirmed in actual code (services-core/Crm/Api.agda) and against docs/SPEC.md §13. (1) Error envelope: errJson (Api.agda:70) emits flat {"error":"<string>"}; §13 mandates {"error":{"code","message"}} with codes not_found/validation/forbidden/unauthorized/conflict/internal — the Err->status mapping (errResp, lines 72-78) carries no structured code. (2) Routes: route (lines 184-193) serves /parties,/accounts,/charge, not /api/v1/<entity-plural-kebab> with {uuid} sub-resources. (3) Pagination: getParties/getAccounts (lines 168,171) -> listJson -> IM.toList (line 124) dump the whole table, no ?limit=&cursor=, no next_cursor. Bonus violation the report undersold: §13 says "наружу — только uuid" but partyJson/accountJson (lines 105-111) leak internal bigint id, commit returns {"id":...} (line 84), and postCharge takes the internal numeric acc id (line 158) — internal id both leaks and is required as input. Proposed-fix primitive is real: IndexedMap.entriesFrom exists (src/Agdelte/Storage/IndexedMap.agda:111-113), so cursor pagination (cursor=afterId) is cheap. Severity kept at medium, not raised: this is a self-labeled Phase-5 MVP (header line 3), a contract-conformance/breaking-change-debt issue, not a correctness/security defect. The 'latent DoS' angle is weak — single-operator, single-tenant, Linux-only MVP; response size is bounded by the one operator's own data, no external attacker amplification — so I discount that part of the rationale. The compounding breaking-change argument is sound: clients written against today's shape are incompatible with §13 on routes, error codes, id-exposure, and pagination simultaneously."
        }
      },
      {
        "title": "Corrupt length-header byte misclassified as torn tail → silent truncation of all committed records after it; walOpen reports success",
        "file": "src/Agdelte/FFI/FileSystem.agda:114-116",
        "severity": "high",
        "scenario": "WAL on disk holds three committed transaction records, e.g. `12:<...>` `5:<...>` `30:<...>`. A single-byte corruption (bit flip, sector rot, half-written block from an earlier crash that left a STALE byte in the middle of an otherwise-replaced file) changes the second record's header from `5:` to `95:` (digit '5'→'9' is one bit). Now `n=95` but `BS.length rest` for that record is much less than 95. The parser hits `BS.length rest < n` → `Just (reverse acc)`: it COMMITS only record #1, silently DROPS record #2 AND record #3 (everything after the corruption), and returns `Just`. walOpen then succeeds and the server starts with a truncated state, having lost durably-acked transactions, with no error logged.",
        "rationale": "The byte-framing design claims (FileSystem.agda:61-65, WAL.agda:16-23) that a too-short body is only ever a TORN TAIL at the physical end of the log, so dropping it is safe; mid-stream corruption is supposed to be caught by the `<digits>:` boundary check and turned into `Nothing`→die. But an inflated length in a header that IS syntactically `<digits>:` is NOT caught: the code cannot distinguish 'declared length exceeds remaining because the writer was interrupted at EOF' from 'declared length is wrong because a header byte was corrupted mid-stream'. Both take the `Just (reverse acc)` branch. This is materially worse than the documented 'no per-record CRC' deferral: that deferral is about UNDETECTED corruption; here the corruption actively and silently discards committed records that follow it and reports clean recovery. With no CRC there is no way for the parser to know record #2's body length is wrong, so it trusts the inflated header and truncates.",
        "fix": "Distinguish torn-tail from mid-stream corruption structurally: a too-short body is only legitimate as the VERY LAST bytes of the file. Concretely, when `BS.length rest < n`, return `Just` (torn tail) ONLY if this is the final record attempt with no trailing bytes expected; otherwise treat as corruption. The robust fix is a per-record CRC/checksum (promote the documented deferral) or, cheaper, a trailing end-marker/record-count so an inflated mid-stream length fails the boundary invariant and yields `Nothing`→die instead of silent truncation.",
        "dimension": "durability",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "medium",
          "note": "Confirmed in actual code at src/Agdelte/FFI/FileSystem.agda:113-116 (the cited range is correct). walGo parses each record left-to-right; when a header parses as syntactically valid `<digits>:` but the decoded length n exceeds `BS.length rest`, it takes the `if BS.length rest < n then Just (reverse acc)` branch (line 114-115). This commits records before the corruption and silently drops this record AND every record after it, returning Just. walOpen (WAL.agda:112-118) then treats Just as clean recovery and starts the server with truncated state, no error. Verified there is NO CRC/checksum/end-marker/record-count anywhere (grep empty), so an inflated mid-stream length header (e.g. a one-bit digit flip '5'->'9' turning `5:` into `95:`) cannot be distinguished from a legitimate torn EOF tail, and defeats the advertised 'mid-stream corruption -> nothing -> die' guard documented at WAL.agda:19-20 / FileSystem.agda:61-65. So the mechanism and consequence are real and accurately described. Severity downgraded high->medium: this is fundamentally a manifestation of the ALREADY-DOCUMENTED 'no per-record CRC in the WAL' deferral — without a checksum the parser cannot validate ANY header field, and the proposed fix (per-record CRC) is literally that deferral. The 'materially worse than documented' delta is genuine but modest: it requires actual on-disk byte corruption of a header digit (bit rot / stale half-written byte), not a normal crash — in a normal crash a too-short trailing record IS a legitimate torn tail that the code correctly drops. Single-writer serial design never emits a torn record mid-stream, so absent media corruption this branch only fires at true EOF. Worth fixing (promote the CRC deferral) but it is a sharper characterization of a known gap rather than a novel high-severity bug."
        }
      },
      {
        "title": "No uniqueness invariant on external uuid (pUuid/eUuid/aUuid/acUuid) — duplicates are silently storable and the API offers no byUuid disambiguation",
        "file": "services-core/Crm/Identity.agda:44",
        "severity": "medium",
        "scenario": "genUuid (Api.agda:50-60) reads /proc/sys/kernel/random/uuid per request. There is no store-level check that the generated uuid is unique before SetParty/SetAccount is emitted, and IndexedMap has no uuid index (Store.agda:27 defers byUuid to 'the API layer', which never implements it). If the RNG source ever repeats (e.g. a restored VM snapshot / cloned container replaying the same /proc entropy, or a future code path that accepts a client-supplied uuid), two distinct internal ids end up with the same external uuid. External callers that key on uuid (the documented external-id contract §13) then conflate two entities, and there is no server-side rejection.",
        "rationale": "byUuid-lookup-not-implemented is a documented deferral, but the *absence of a uniqueness invariant entirely* is a domain-invariant hole, not just a missing read path: the store can hold a state no valid external client could address unambiguously. Because uuid is the addressable identity in §13, this is a correctness gap, not just an ergonomics one.",
        "fix": "Add a uuid secondary index (extract = [hash uuid]) to the relevant IndexedMaps and a guardT in the create commands that aborts Conflict if the uuid is already present, OR document explicitly that uuid is non-unique and remove it from the external-addressing contract.",
        "dimension": "correctness",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Facts confirmed in code. services-core/Crm/Identity.agda:44 declares `pUuid : Uuid` (=String) with no uniqueness constraint; same for eUuid (line 63), aUuid (line 78), acUuid (line 111). Crm/Store.agda:27 comment defers byUuid hashing "to the API layer"; emptyBase (lines 62-69) builds only FK ℕ-indexes (extActEng/extPartEng/extPartParty) — no uuid index exists anywhere. Crm/Commands.agda:60-76 (createParty/createEngagement/createAccount) emit SetParty/SetAccount with the caller-supplied uuid and contain NO guardT/conflict check on uuid. genUuid is at Crm/Api.agda:49-60 (not Api:50-60 as cited — close), reading /proc/sys/kernel/random/uuid per POST (lines 133, 150). So the store genuinely cannot reject a duplicate uuid, and per SPEC §13 (SPEC.md:564 "Наружу — только uuid. bigint id никогда не покидает сервер") uuid IS the sole external address — and the parked Postgres DDL had `uuid ... unique` (SPEC.md:210,219) which the WAL path silently drops. The invariant gap is therefore real and distinct from a mere missing read path.

BUT severity is overstated as medium; I downgrade to low: (1) This is materially the already-documented byUuid deferral (plan-crm-core.md:156-159, storage-model.md:65-66) viewed from the write side. (2) Exploitability is not concrete. The uuid source is the kernel CSPRNG (v4 UUID); collision probability is cryptographically negligible, so two records sharing a uuid essentially cannot arise in normal single-operator operation. (3) The report's trigger scenarios are speculative: the "client-supplied uuid" code path does NOT exist (every handler calls genUuid itself — postParty Api.agda:133, postAccount:150), and the VM-snapshot/cloned-entropy angle is hypothetical (kernel reseeds; /proc uuid is not a replayable static value). So there is no input/sequence today that drives the system into the duplicate-uuid state. It is a latent design-completeness gap (no server-side uuid uniqueness invariant before uuid becomes externally addressable), worth a one-line note when byUuid is finally implemented, but not a present correctness bug given the CSPRNG source and absence of any client-supplied-uuid path. Proposed fix (add uuid secondary index + guardT Conflict in create commands) is the right shape and would naturally land together with the deferred byUuid index."
        }
      },
      {
        "title": "genUuid has no failure handling: any /proc read error fails every entity-creating write",
        "file": "services-core/Crm/Api.agda:55 (genUuidHS uses withFile/hGetLine, no try) used in postParty:133 and postAccount:150",
        "severity": "medium",
        "scenario": "On a host where /proc/sys/kernel/random/uuid is unavailable or restricted (hardened kernel, container with masked /proc, or a transient read error/EOF), hGetLine throws. The exception propagates out of the POST handler; Agdelte.Http.toWaiApp catches it and returns 500 'Internal server error'. So EVERY POST /parties and POST /accounts fails — a single dependency takes down all entity creation, with the operator getting only an opaque 500.",
        "rationale": "This is the documented Linux-only assumption, but the failure mode is undifferentiated (generic 500 from the HTTP layer, logged to stderr as a raw exception show) and total: there is no fallback uuid source and no graceful degradation. The handler also doesn't wrap genUuid in tryCatch to surface a clean error envelope. Acceptable for a prototype but worth flagging because it converts a missing-file into a full write outage.",
        "fix": "Wrap genUuid in tryCatch and return a clean 503 {"error":"id source unavailable"} on failure; consider a userspace UUIDv4 generator (random bytes) so writes don't depend on a single proc file.",
        "dimension": "security",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Confirmed the code mechanism, though the cited line numbers are slightly off. genUuidHS is at services-core/Crm/Api.agda:55-58 (the FOREIGN block; postulate at :49-50, COMPILE at :60), using withFile "/proc/sys/kernel/random/uuid" ReadMode + TIO.hGetLine with NO exception handling. It is called before commit in postParty (Api.agda:133) and postAccount (Api.agda:150). The HTTP layer is Agdelte.Http.toWaiApp (hs/Agdelte/Http.hs:92-115): it wraps the handler in `try`, on SomeException logs `show e` to stderr and returns generic 500 "Internal server error" (lines 113-115). So an IO error reading the uuid proc-file does propagate as an opaque 500 for every POST /parties and POST /accounts. Accurate detail: postCharge (Api.agda:155-164) does NOT call genUuid, so charges keep working — the outage is specific to entity creation, matching the finding. WAL storage failures already return a clean 503 (Api.agda:86), so the 500-vs-503 inconsistency the finding notes is real but cosmetic. SEVERITY DOWNGRADED to low: this sits directly on top of an explicitly documented deferral ('genUuid is Linux-only (/proc/sys/kernel/random/uuid)') and the design intent states Target = Linux only, single operator. On a normal Linux host that file is a kernel-synthesized virtual file always present and world-readable; it does not transiently fail in normal operation, so the 'transient EOF/read error' scenario is largely hypothetical. The materially-worse-than-documented bar is not really met: this is an error-ergonomics/robustness nit (no userspace UUIDv4 fallback, opaque 500 instead of a clean 503 envelope), not a latent correctness bug. Proposed fix (wrap in tryCatch -> 503, or generate UUIDv4 from random bytes) is reasonable hardening but optional for the MVP."
        }
      },
      {
        "title": "GET /parties returns soft-deleted rows — contract says reads must filter them",
        "file": "services-core/Crm/Api.agda:167 (getParties) and :124 (listJson uses IM.toList) vs IndexedMap.agda:110 note 'includes soft-deleted rows — a live page filters them at the call site (P4)'",
        "severity": "low",
        "scenario": "If/when a soft-delete is wired for parties (pDeletedAt set), `GET /parties` still lists them: listJson maps over IM.toList with no live-filter, contradicting the documented P4 invariant that the call site filters soft-deleted rows. A 'deleted' party's name and uuid (PII) would keep leaking from the list endpoint.",
        "rationale": "Currently no command sets pDeletedAt (createParty passes nothing, and only hard-delete ops exist), so it is not yet exploitable — hence low. But the read path violates the stated soft-delete contract, so the moment a soft-delete setter is added this becomes a PII-after-deletion leak. Worth fixing now while the surface is small.",
        "fix": "Filter `live (pDeletedAt p)` in getParties/listJson (and any future list endpoints) so soft-deleted rows never appear in API reads, honoring the P4 invariant.",
        "dimension": "security",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Confirmed real, correctly rated low. getParties (services-core/Crm/Api.agda:167) calls listJson partyJson (parties b); listJson (Api.agda:124) does `map (...) (IM.toList m)` with no live-filter. The P4 contract is documented in src/Agdelte/Storage/IndexedMap.agda:109-110: "includes soft-deleted rows — a live page filters them at the call site (P4)" (note: the finding's path "IndexedMap.agda:110" omits the src/Agdelte/Storage/ prefix; line ~109-110 is correct). Party.pDeletedAt exists (Crm/Identity.agda:49) and the `live` helper (Crm/Commands.agda:40-42) IS used for FK-existence checks in commands (e.g. Commands.agda:88) but NOT in either read endpoint (getParties/getAccounts). Not currently exploitable: createParty (Commands.agda:60-64) always emits pDeletedAt=nothing, no command ever sets it to `just`, and DelParty/DelAccount are hard deletes (apply at Store.agda:114 = IM.delete, physically removes the row). So today a deleted party is gone, not soft-hidden — no PII leak yet. The finding is a forward-looking contract-consistency fix: if a soft-delete setter is ever added, the read path would leak soft-deleted rows. Proposed fix (filter `live (pDeletedAt p)` in listJson/getParties) is correct and matches the documented invariant. Same gap applies to getAccounts (aDeletedAt). Severity low is appropriate — latent, defensive, small surface."
        }
      },
      {
        "title": "Soft-delete is modeled and checked everywhere but no command can ever set it; DELETE is hard-only — contradicts SPEC §5.7 retention/anonymization",
        "file": "services-core/Crm/Commands.agda:132",
        "severity": "medium",
        "scenario": "hardDeleteEngagement emits DelActivity/DelParticipation/DelEngagement (Commands.agda:135-137), and apply (Store.agda:114-118) calls IM.delete which physically removes the rows. No command anywhere sets pDeletedAt/eDeletedAt/aDeletedAt to (just now) — grep finds none. Yet every entity carries a Maybe ℕ deletedAt (Identity.agda), FK checks reject non-live rows (Commands.agda:86,88,111), slotFree filters on live (Commands.agda:105), and IndexedMap.entriesFrom/P4 documents 'includes soft-deleted, filter at call site'. So the live-check machinery is dead code and the only deletion path is irreversible hard delete.",
        "rationale": "SPEC §5.7 mandates soft-delete + retention and explicitly 'анонимизация вместо удаления' for 152-ФЗ compliance, and storage-model.md notes byUuid 'может резолвить и удалённую запись'. The current code instead destroys rows. Adding real soft-delete later isn't a drop-in: it needs new ops (e.g. a deletedAt-setting Set variant or a SoftDel op), changing hardDeleteEngagement's cascade semantics, deciding index visibility of soft-deleted rows, and the entriesFrom live-page top-up the doc only hand-waves ('добирать до limit живых'). That cascade of changes is a rewrite of the deletion/retention story, not an addition — a latent corner worth surfacing now while only one command deletes.",
        "fix": "Decide soft- vs hard-delete per entity before more commands exist. For §5.7-governed entities add a soft-delete op that sets deletedAt and keep the row; reserve hard delete for genuinely transient data. Then make the live-checks and entriesFrom live-page filtering meaningful, and spec how byUuid resolves tombstoned rows.",
        "dimension": "architecture",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Confirmed in actual code. Three Identity records carry Maybe ℕ deletedAt (Identity.agda:49 pDeletedAt, 66 eDeletedAt, 83 aDeletedAt). The `live` helper (Commands.agda:40-42) gates FK checks (Commands.agda:86,88,111) and slotFree (105). But NO command sets any *DeletedAt to (just now): createParty/createEngagement (Commands.agda:64,70) hardcode `nothing`, bookSession (114) hardcodes `nothing`, and there is no Set-with-deletedAt variant anywhere (grep over services-core/server/Api finds zero `just`-setting site). The only delete path is hardDeleteEngagement (Commands.agda:132-137) emitting Del* ops, which apply (Store.agda:114-118) routes to IM.delete = physical removal. So the live-check guards are currently unreachable (they never fire) and deletion is irreversible — accurate as reported, and it does contradict SPEC §5.7 (анонимизация вместо удаления, SPEC.md:387,531).

Severity downgraded medium->low: (1) This is an in-progress Phase-4 build; the live guards are correct defensive code, not harmful dead code — nothing at runtime misbehaves. (2) No safety/correctness defect exists in the commands that DO ship.

The genuinely sharp sub-issue the report undersells and I add: the docs OVERCLAIM the fix is a drop-in. plan-crm-core.md:129 states 'Soft-delete = обычный Set* с *DeletedAt, уже есть' (already there) — false: no Set*-with-deletedAt is exposed. More importantly, storage-model.md:123-126 claims the index extractor 'пропускает удалённые' (skips soft-deleted so byIndex returns only live), but the real extractors extActEng/extPartEng/extPartParty (Store.agda:30-37) return the FK UNCONDITIONALLY — they do not consult deletedAt. So even if a Set* set deletedAt, the row would remain visible in every reverse-index query; slotFree (Commands.agda:105) happens to re-filter `live` by hand, but hardDeleteEngagement's cascade (135-136) and any future byIndex reader would see tombstones. Thus adding soft-delete is not the documented one-liner: it requires changing the three extractors (or adding a live-index), a Set*/SoftDel op variant, and the entriesFrom live-page top-up that storage-model.md:130 only hand-waves. The report's file:line citations are all correct."
        }
      },
      {
        "title": "app/migrations/0001_identity.sql ships a full Postgres/RLS schema inside the 'current' app layer, while the chosen engine is WAL+in-memory — an extractability/consistency hazard",
        "file": "app/migrations/0001_identity.sql:1",
        "severity": "low",
        "scenario": "git mv'ing services-core/packs/app to a new repo (the stated extractability goal) carries app/migrations/0001_identity.sql (tenant, app_user, party, account, RLS, tenant_id — SPEC §5.2/§5.7) plus app/README.md which says app's purpose includes 'migrations/ applied by the agdelte migration runner (Agdelte.Server.Migrate)'. A reader of the extracted repo sees an authoritative SQL schema and a migration runner and reasonably concludes storage is Postgres — contradicting ADR 0001 / CLAUDE.md ('WAL + in-memory, НЕ Postgres') and the live Crm.Store/WAL code.",
        "rationale": "CLAUDE.md says the SQL-oriented SPEC parts are the PARKED Postgres path, not the current branch. But the parked artifact lives in the active app/ layer with a README presenting it as app's job, with no marker that it's deferred. On extraction this is the most misleading file in the tree about which storage engine is real. It also duplicates the source of truth for entities (SQL schema vs Crm.Identity records), which will drift.",
        "fix": "Either move 0001_identity.sql + the Postgres-migration framing out of app/ into a clearly-parked location (e.g. docs/postgres-spike/ next to POSTGRES-SPIKE.md), or add a header/README note that it is the deferred scale-out path and NOT used by the WAL engine. Keep app/'s README describing the WAL-backed reality.",
        "dimension": "architecture",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Confirmed in actual code. app/migrations/0001_identity.sql exists (8592 bytes) and ships a full Postgres schema: tenant, app_user, party, account, profile, channel_handle, segment, party_segment, consent — with bigserial PKs, tenant_id FKs, ENABLE ROW LEVEL SECURITY + per-table tenant_isolation policies using current_setting('app.current_tenant') (lines 23-175). No header marks it as the parked/deferred Postgres path. app/README.md (lines 7-8) presents migrations affirmatively as app's job: "migrations/ — ordered NNNN_name.sql files applied by the agdelte migration runner (Agdelte.Server.Migrate)" — and grep for parked|deferred|postgres|WAL|in-memory across app/ returns NOTHING, so there is zero in-tree marker that this is the parked path. Meanwhile the LIVE engine is WAL+in-memory: services-core/Crm/Store.agda, src/Agdelte/Storage/WAL.agda, src/Agdelte/Storage/AppStore.agda, ADR docs/adr/0001-storage-wal-in-memory.md. The migration runner Agdelte.Server.Migrate does exist (src/Agdelte/Server/Migrate.agda) so the README's claim is technically wired to a real, but parked, framework path. Source-of-truth duplication is real AND already diverging: services-core/Crm/Identity.agda is the live entity source (records Party/Account/Engagement/Participation), and its header (line 7) explicitly states "Tenancy omitted for the single-operator MVP" — directly contradicting the SQL file's tenant_id/RLS multitenancy. So the two schemas have already drifted on the central tenancy axis. This matches the design-intent note 'Postgres path parked' and is on the documented-deferral list, but it is materially worse than 'just documented' on the extractability dimension: per CLAUDE.md's explicit git-mv extractability goal, app/ carries this misleading file with an affirmative README and no deferral marker, and the drift is concrete (tenancy present vs omitted), not hypothetical. Not a runtime bug (the SQL is never executed by the WAL engine; nothing crashes), so 'low' is the honest severity — borderline informational, kept at low because of the affirmative README framing and the already-realized schema divergence. Proposed fix (move under docs/postgres-spike/ next to POSTGRES-SPIKE.md, or add a deferred-path header + WAL-reality note to app/README.md) is appropriate. Cited file:line app/migrations/0001_identity.sql:1 is correct."
        }
      },
      {
        "title": "IndexedMap secondary indexes never reclaim empty key buckets — monotonic growth under churn, sharpened by the planned byUuid-hash index",
        "file": "src/Agdelte/Storage/IndexedMap.agda:50",
        "severity": "low",
        "scenario": "removeKey (IndexedMap.agda:50-52) writes the filtered id-list back via NM.insert even when it becomes []: NM.insert key [] im. So every index key ever touched stays in the IdxMap forever as an empty bucket. Sequence: book then hardDeleteEngagement 1e6 sessions across distinct engagements → activities.byEngagement holds 1e6 empty buckets. With the documented next step (byUuidHash : String → ℕ index, one bucket per distinct uuid hash), every created-then-deleted entity leaves a permanent bucket; over a long-lived single-instance deployment this is an unbounded in-memory leak with no rotation (WAL-only, no snapshot to compact through).",
        "rationale": "The store is in-memory and long-lived by design (no snapshot/compaction), so anything that grows monotonically with total historical writes — not live rows — is a real concern, unlike a Postgres index that VACUUMs. byUuid-by-hash is explicitly the planned index, which is exactly the high-cardinality case that makes this bite. It's not catastrophic (empty lists are small) but it's a design corner the abstraction hides, so callers can't see or fix it.",
        "fix": "In removeKey, NM.delete the key when the resulting id-list is [] instead of inserting an empty list. This keeps the index invariant (absent key ≡ empty bucket) and reclaims space. Verify byIndex already treats a missing key as [] (it does, via fromMaybe []).",
        "dimension": "architecture",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Confirmed in real code. src/Agdelte/Storage/IndexedMap.agda:50-52 `removeKey` always does `NM.insert key (filterᵇ … list) im`, never NM.delete — so when the filtered id-list becomes [], an empty bucket is written back and persists forever. Empty buckets accumulate monotonically with total historical index-key touches (not live rows). delete (line 96-100) → slotDelete (68) → removeKeys (57) → removeKey, so hard delete drives this. byIndex (line 105) already maps a missing key to [] via `fromMaybe []`, so the proposed fix (NM.delete the key when result is []) is behaviorally invariant and implementable: NM.delete exists (NatMap.agda:28,61). The 'sharpening' claim checks out: byUuidHash is the explicitly documented next index (docs/plan-crm-core.md:158, 'hashString : String → ℕ + индекс byUuidHash'), high-cardinality. Concrete domain path: services-core/Crm/Store.agda activities has byEngagement FK index (extActEng, line 30-31); 1e6 created-then-hardDeleted engagements ⇒ 1e6 permanent [] buckets in activities' IdxMap. Not catestrophic (Data.Map.Strict entry per stale key is small) and the store is WAL-only with no snapshot/compaction (ADR 0001), so this is genuine unbounded-with-history growth. NOT covered by design intent or any documented deferral (the 'WAL growth/rotation' deferral is on-disk WAL, not in-memory index residue). Extra wrinkle the report omits: restart/replay does NOT clear it — replay re-applies the same insert/delete ops and recreates identical empty buckets, so there is no recovery path. Severity 'low' is honest and correct: real design corner, small absolute footprint, no live-data correctness impact (byIndex still returns correct [])."
        }
      },
      {
        "title": "Integer overflow in the WAL length-prefix digit fold (Int, not Integer) — corrupt huge header can wrap to a negative length and re-parse the same bytes",
        "file": "src/Agdelte/FFI/FileSystem.agda:113-116",
        "severity": "medium",
        "scenario": "A corrupt record header contains a very long digit run, e.g. `99999999999999999999999:` (≥20 nines). `foldl' (\a w -> a*10 + ...) 0 ...` is computed at type `Int` (forced by `BS.length rest < n`, `BS.take n`, `BS.drop n`, all `Int`). The fold silently overflows and can land on a NEGATIVE `n`. Then `BS.length rest < n` is `False` (a non-negative length is never < a negative n), so control falls to the else branch: `walGo (BS.drop n rest) (decodeUtf8 (BS.take n rest) : acc)`. `BS.take negative` = empty, `BS.drop negative` = the whole `rest`. So it appends a spurious empty record and recurses on `rest` UNCHANGED past the consumed `:`. Depending on the following bytes this either fabricates bogus empty records or, if `rest` again starts with the same digit pattern, fails to make progress.",
        "rationale": "The parser assumes the folded length is a faithful non-negative byte count. `Int` overflow breaks that assumption: a corrupt/adversarial header can drive `n` negative and defeat the `BS.length rest < n` torn-tail guard, producing fabricated records (corrupting recovered state) instead of a clean `Nothing`→die. Unlike the Agda-side `readℕ`/`readDigits` which fold into unbounded `ℕ`, this FFI fold is bounded `Int`. The single-writer never EMITS such a header, but recovery must be robust to a CORRUPT log, which is exactly the threat model this parser exists for.",
        "fix": "Fold into `Integer` (unbounded) and bound-check before narrowing: `let n = foldl' (\a w -> a*10 + toInteger (w-48)) 0 (BS.unpack digits) in if n > toInteger (BS.length rest) then Just (reverse acc) else let ni = fromInteger n in walGo (BS.drop ni rest) ...`. Also cap the digit count (a header with > ~19 digits is by definition corruption → `Nothing`) to avoid pathological large allocations from `BS.take`.",
        "dimension": "durability",
        "verdict": {
          "skill": "",
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Confirmed in real code. src/Agdelte/FFI/FileSystem.agda:113 folds the length prefix at type Int (forced by BS.length/BS.take/BS.drop being Int at lines 114,116-117), not Integer. I reproduced the chain end-to-end in GHC: a digit run of 19 nines overflows Int64 to -8446744073709551617; with a negative n, `BS.length rest < n` is False (line 114) so control falls to the else branch where `BS.take n rest`="" (spurious empty record) and `BS.drop n rest`=whole rest unchanged past the consumed ':'. Input `9999999999999999999:5:world` yields `Just ["","world"]` — the parser returns Just (success) instead of Nothing, so walOpen (src/Agdelte/Storage/WAL.agda:114-118) replays instead of calling `die`. This is a genuine hole in the parser's stated contract ("a boundary that isn't <digits>: => Nothing => refuse to start"): a digit-only-but-overflowing header is accepted. The proposed fix (fold into Integer, bound-check before narrowing, cap digit count) is correct and cheap.\
\
Downgraded from medium to LOW because the report overstates impact: (1) the fabricated record is EMPTY, and an empty payload decodes to ZERO ops (WAL.agda:91-92 `go _ acc [] = just acc`), so NO bogus op is injected into recovered state — the report's "corrupting recovered state" / "fabricates bogus records" is wrong for the realistic case; the real harm is "fails to die on a corrupt log + silently drops the misframed region." (2) Most digit-count landings overflow to a POSITIVE n (e.g. a 20-digit corrupt header gave n=4.6e18) which simply hits the already-documented torn-tail `Just (reverse acc)` drop path — not novel. (3) Reachable only via arbitrary WAL bit-corruption producing a >=19-digit ASCII run before a ':'; the single serial writer never emits such a header (real byte lengths need far fewer digits). This sits squarely inside the already-DOCUMENTED deferral "no per-record CRC/checksum in the WAL" — the WAL is by design not robust to arbitrary corruption — so it's a refinement of a known gap, not a new class of risk. Worth fixing (Integer fold + 19-digit cap) but not medium-grade."
        }
      },
      {
        "title": "Recovery fuel-exhaustion path in applyRecord silently truncates a record's ops on a pathological (but reachable-under-corruption) body, since `nothing` from out-of-fuel is treated as whole-record corruption→die only, not partial",
        "file": "src/Agdelte/Storage/WAL.agda:88-98",
        "severity": "low",
        "scenario": "applyRecord uses `fuel = length (toList body)` and claims each op consumes ≥2 chars so fuel suffices. But `readField` accepts a zero-length payload: header `0:` is exactly 2 chars and yields payload "" with rest advanced by 2. A corrupt-but-syntactically-valid inner stream like `0:0:0:...` (n bytes of `0:` pairs) consumes 2 chars per step and decodes payload "" each time; `walDeserializeOp ""` (decodeOp on empty) returns `nothing` (Store.agda:157-158), so go returns `nothing` → replayAll returns `nothing` → walOpen dies. That part is fine (fail-closed). The latent issue: the fuel bound's soundness ('each op consumes ≥2 chars') relies on readField NEVER returning a (payload,rest) where rest length didn't strictly decrease. readField with payload length n returns rest = drop n after the header; if a corrupt header declared n larger than remaining, `splitAt n rest` in Wire.readField (Wire.agda:61) returns (all-of-rest, []) WITHOUT error, so the op 'succeeds' consuming everything and the next go sees [] → `just acc`. That means a single intact record whose inner framing was corrupted to over-declare a field length is accepted as a VALID (truncated) op set rather than rejected.",
        "rationale": "Inner framing (Wire.readField) uses `splitAt n` which clamps silently when n exceeds available chars rather than failing. Inside a 'complete' record the design assumes inner framing is exact, but a record can be byte-complete yet internally corrupt (corruption inside the body that doesn't touch the OUTER `<byteLen>:` boundary). In that case an over-declared inner length is accepted (partial split) and the trailing real ops in the same record are dropped, with the op decode possibly still succeeding → state silently loses ops from that transaction while walOpen reports success. This is a narrower, lower-probability variant of finding #1 (no inner CRC either), hence low severity, but it shows the inner char-framing shares the same 'over-declared length is not detected' weakness.",
        "fix": "Make Wire.readField reject when the declared field length exceeds the remaining chars: after `splitAt n rest`, require `length (proj₁ sp) ≡ n` else `nothing`. That turns inner over-declared lengths into corruption→die instead of silent partial decode, matching the fail-closed intent.",
        "dimension": "durability",
        "verdict": {
          "isReal": true,
          "confidence": "high",
          "severityAdjusted": "low",
          "note": "Verified the mechanical claim, downgraded the impact claim. Wire.agda:61 confirmed: readField does `let sp = splitAt n rest in just (fromList (proj₁ sp) , proj₂ sp)` with NO check that `length (proj₁ sp) ≡ n`. Data.List.splitAt clamps silently when n > length rest, returning (all-of-rest, []), so an over-declared inner field length is not detected by the framing layer — accurate. applyRecord (WAL.agda:88-98) confirmed: on readField success the payload feeds walDeserializeOp; if that returns `just op`, recursion continues on `rest`, and `go _ acc [] = just acc` accepts the record. So the framework relies entirely on the op decoder to reject an over-declared/merged payload. The fuel argument (`each op consumes ≥2 chars`) is about totality only and is unaffected by this.

BUT the headline outcome — 'silently truncates a record's ops', i.e. record accepted while REAL trailing ops are dropped — is NOT reachable with either registered codec, so the data-loss scenario is overstated. To drop trailing ops you need an over-declared length on a NON-final op AND the merged payload (which now contains the next op's `<digit>:<TAG>…` framing bytes) must still decode to `just op`. CRM decodeOp (services-core/Crm/Store.agda:156-171): Del* uses decℕ which rejects the ':' it swallows; Set* uses Wire.runR (Wire.agda:88-92) which REQUIRES all input consumed, so swallowed framing bytes → nothing. AppStore deserializeOp (src/Agdelte/Storage/AppStore.agda:479-600): splitPipe + exact-arity matches (`… ∷ []`), so swallowed pipe/digit bytes change field count or make a numeric field non-numeric → nothing. In every case the over-declared non-final field → `nothing → die` (fail-closed). The only accepted-without-error case is over-declaring the FINAL op's length, where splitAt clamps to exactly the correct payload and the correct op decodes — zero ops lost. The `0:0:0:…` example the finding gives also just dies (decodeOp ""/deserializeOp "" both = nothing), as the finding itself concedes.

So: real latent framing weakness (inner over-declared length is not self-detected; integrity is delegated to every codec being trailing-byte-strict), but not an exploitable silent-data-loss bug with the code that exists. The proposed fix (after `splitAt n rest`, require `length (proj₁ sp) ≡ n` else nothing) is correct, cheap, and makes the fail-closed contract self-enforcing instead of codec-dependent — worth doing as defense-in-depth. Severity low (hardening), bordering not-a-bug for actual behavior."
        }
      }
    ]
  }
}
